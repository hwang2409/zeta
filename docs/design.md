# zeta design

Re-anchor of the WIKI-361 plan (2026-08-19, Henry decision): the harness is a
standalone project, not a Wiki backend retrofit. It builds its own agent loop
end to end, pi-style, and does NOT spawn claude/codex subprocesses — the
earlier plan's CLI stream-json and app-server seams are dropped along with the
raw-API ban that motivated them. Full context: Wiki vault note
`wk-owned-agent-loop-design` (module map, pi citations, risks); this doc is
the harness-native distillation.

## Provider seam (pi-style, plan-authenticated)

- **Claude**: direct Anthropic Messages API. Auth via Claude Pro/Max
  subscription OAuth (refresh + access tokens) is the default and the only
  path used interactively. A raw `ANTHROPIC_API_KEY` is an explicitly opt-in
  path (ZETA-87) that exists for evaluation and automation — benchmark
  rollouts run far past any consumer plan's allowance — never as an implicit
  fallback: the key must be paired with `ZETA_ALLOW_API_KEY=1` or it is
  ignored, so a developer who merely has the variable exported for unrelated
  tools does not silently start spending API credits instead of plan quota.
  pi reference: `packages/ai/src/providers/anthropic.ts`,
  `packages/ai/src/auth/oauth/anthropic.ts`,
  `packages/ai/src/api/anthropic-messages.ts`.
- **Codex**: direct ChatGPT backend-api (Responses-style, SSE/WebSocket).
  Auth via ChatGPT plan OAuth with account id derived from the access token.
  pi reference: `packages/ai/src/providers/openai-codex.ts`,
  `packages/ai/src/auth/oauth/openai-codex.ts`,
  `packages/ai/src/api/openai-codex-responses.ts`.
- One provider-neutral stream contract: assistant deltas, thinking, tool-call
  deltas, usage. The loop is blind to provider wire formats.
- Keep provider-native prompt-cache identity where the API allows it (we own
  the context, so cache reuse is ours to manage — an advantage the
  subprocess-seam plan had to give up).

## Loop ownership (what the harness owns)

- `AgentLoop`: turns, steering, follow-ups, tool execution, stop conditions.
  One completion per provider call; tool calls stop at the harness boundary.
- `ConversationStore`: append-only JSONL, parent-linked entries, explicit
  compaction markers, torn-tail repair, replay on resume. Derive context from
  the active branch, never from provider history.
- `ContextAssembler` + `CompactionPolicy`: token budget, retained tail,
  no-tools summary completion, block on failed summarization. Never compact
  between tool_call and tool_result.
- `ToolRegistry`: one registry across providers; schema validation, abort
  handling, sequential/parallel execution, pre-execution hook.
- `ApprovalPolicy`: allow / deny / ask with durable pending requests (stronger
  than pi's hook-only model — this we keep from the Wiki design).
- Sessions: `~/.zeta/sessions/` (own home, not ~/.wiki), versioned schema.
- TUI: full-screen alternate-buffer renderer with a scrollable transcript,
  sticky composer, and one pinned footer row. It uses prompt_toolkit + rich and
  restores the user's terminal on exit or crash.

## Isolation

- No imports from the wiki repo. Wiki integrates later behind a flag by
  consuming this package — never the reverse.
- No shared state with ~/.wiki, ~/.claude, ~/.codex beyond READING the OAuth
  credential stores those apps maintain (with consent, documented per
  provider).

## Ticket ladder (ZETA prefix)

| Ticket | Contract | Depends |
|---|---|---|
| ZETA-1 | Core types + ConversationStore (append-only JSONL, parent links, replay, torn-tail repair) + AgentLoop skeleton driven by a deterministic fake backend + tests | — |
| ZETA-2 | Anthropic backend: subscription OAuth (reuse/refresh existing Claude login), Messages API stream, provider-neutral events; single-completion boundary test | ZETA-1 |
| ZETA-3 | Codex backend: ChatGPT plan OAuth, backend-api Responses stream; same boundary test | ZETA-1 |
| ZETA-4 | ToolRegistry + first tools (read/list/exec with session cwd defaults) + pre-execution hook | ZETA-1 |
| ZETA-5 | ApprovalPolicy with durable pending requests + resume re-emit | ZETA-4 |
| ZETA-6 | ContextAssembler + CompactionPolicy (budget, retained tail, summary completion, digest) | ZETA-2 or ZETA-3 |
| ZETA-7 | Session resume (--continue/--resume), provider-transport recreation | ZETA-6 |
| ZETA-8 | TUI: sticky composer, patch_stdout pump, status bar, commit-on-newline streaming | ZETA-2/3 |
| ZETA-10 | `zeta login` CLI subcommand: wire existing PKCE OAuth machinery (build_authorization_url + exchange_authorization_code, both providers) into a CLI flow with a local redirect server; store tokens via AnthropicCredentialStore / CodexCredentialStore. Motivation: no login surface today; when both stored tokens revoke (session on another machine invalidates them), users have no in-harness recovery path. | ZETA-2/3 |
| ZETA-11 | Refresh-on-401 in the completion path: when a completion raises AuthError from a 401, attempt a token refresh once and retry the completion; fail loudly on the second 401. Motivation: `access_token()` only refreshes on local expiry — server-side revocation (token invalidated by a Claude/ChatGPT re-login elsewhere) leaves the local file "valid" and the completion 401s with no recovery. | ZETA-2/3 |
| ZETA-12 | `list` tool output cap fragility: the 10,000-char cap in ZETA-4's `list` truncates the accumulated pytest tmp_path parent after ~195 tests, silently breaking `tests/test_tools.py::test_paths_outside_session_cwd_are_allowed` when suite size grows. Either raise/remove the cap for the assertion path, or restructure the test to assert the invariant against a structured result rather than the truncated listing. Blocks nothing today (worked around in the test itself); this ticket fixes the underlying fragility so the workaround can be removed. | ZETA-4 |
| ZETA-13 | Structured tool results: adopt MCP's content-array return shape (`{"content": [{"type": "text"|"image"|"resource", ...}], "isError": bool, "structuredContent": <optional>}`). Replaces today's string returns everywhere in the ToolRegistry. Explicit `truncated: bool` + `full_size` metadata on text blocks — no more silent output caps. Same shape MCP servers already emit; foundation for ZETA-18. | ZETA-4 |
| ZETA-14 | `bash` tool: general shell tool folding in `exec` and replacing `list` entirely (list/git/search all become bash invocations). PERSISTENT per-session cwd — `cd foo && ls` state survives across bash calls within one session (serialized into the store's session state). Structured return: stdout, stderr, exit_code, cwd_after. Session-cwd defaults; explicit per-call override still allowed. Drop `list` from the registry. | ZETA-13 |
| ZETA-15 | `write` tool: write file at path with structured result (bytes_written, sha256, was_created vs was_overwritten). No auto-create parent dirs (fail-loud); explicit `create_parents: bool` flag if needed. | ZETA-13 |
| ZETA-16 | `edit` tool: Anthropic-native str_replace shape — `{"path", "old_string", "new_string"}`. `old_string` must be unique in file, else fail with the count. Cache-friendly (matches what claude/codex expect natively), no diff-parsing surface area. | ZETA-13 |
| ZETA-17 | Streaming tool output: long-running `bash` calls emit stream chunks the TUI renders live. Assistant sees ONE final tool_result on completion (or a canceled marker on abort) — no mid-turn partial tool_results in the model context. Preserves ZETA-8/ZETA-9's ordering invariants and keeps prompt cache reuse clean. Live-preview is a rendering concern only. | ZETA-14 + ZETA-13 |
| ZETA-18 | MCP client: one adapter that connects to any MCP server (stdio + streamable-http transports first), translates each server's tools into zeta's structured-tool-result shape. Per-server auth. Server discovery via a config file at `~/.zeta/mcp.json` (or `ZETA_MCP_CONFIG`). Servers appear as first-class tools in the registry; the assistant does not know they are remote. | ZETA-13 |
| ZETA-19 | `/status` slash command + slash-command primitive: minimal slash dispatcher — parse `/word args` at input time, before it reaches the model. First command is `/status`: session id, provider/model, retained tail, tokens used this session, tokens in current context, compaction-marker count, live pending approvals. Primitive stays small — no full DSL, no hooks. Later slash commands are separate tickets. | ZETA-2/3, ZETA-6 |
| ZETA-20 | TUI polish (OpenCode / Codex parity): status bar, sticky composer polish, markdown syntax colors, tool-call rendering (collapsible), streaming indicator, palette tune. Can ship as multiple rounds — first round targets visual parity with OpenCode's chrome. | ZETA-8 |
| ZETA-21 | Codebase restructure: split the flat `src/zeta/*.py` layout into `core/` (loop, store, context, session, approval+gate merged, abort, fake), `providers/` (transport, auth, anthropic, codex), `tools/` (registry + one file per tool: `read`, `list`, `exec` — current tools split out of today's `tools.py`), `skills/` (folder + a tiny `loader.py` stub for future markdown skills — see notes). `tui/` stays as-is. `types.py` and `cli.py` stay flat at top level. `__init__.py` becomes a public barrel only. Skills semantics locked: claude-code-style markdown files (prompt content + trigger keywords), model discovers and loads on demand, NO per-skill tool-mounting (tools stay mounted at session level). Add two governance tests: `tests/test_import_boundaries.py` (`core` imports nothing from above; `providers` can't import `tools`; `skills` can't import `providers`) and `tests/test_module_limits.py` (per-file line cap, per-dir file cap). NO behavior change — every existing test passes unchanged at the same assertion. SEQUENCING: ship BEFORE starting ZETA-13..20 so subsequent tool/tool-adjacent tickets land in the new layout naturally. | ZETA-7 |
| ZETA-22 | Cross-cutting filesystem sandbox: race-free TOCTOU for tools. Rename-escape, hard-link write, ancestor-symlink walk, and F_GETPATH portability. Depends on: ZETA-15. Motivation: the write tool ships with a best-effort sandbox in ZETA-15; a hardened sandbox is a cross-cutting concern that should apply uniformly across read/list/write/exec. | ZETA-15 |
| ZETA-23 | Prompt-cache observability: normalize cache usage across providers, count cached and uncached tokens, fix cache-aware compaction accounting, expose /status hit rate, and lock Anthropic breakpoints with governance tests. | ZETA-2/3, ZETA-6, ZETA-19 |
| ZETA-24 | Dynamic tool discovery: ToolRegistry auto-discovers tool modules in `src/zeta/tools/` — drop in a `.py` with the standard tool contract, get a tool; built-ins become discovered modules with no special-casing; malformed tool modules fail loudly at construction. No config, no manifests. Non-text transport is flatten-fallback until ZETA-28. | ZETA-13 |
| ZETA-25 | Project context injection: at session start read `~/.zeta/AGENTS.md` then repo-root `AGENTS.md` (fallback `CLAUDE.md`), concatenated into the system prompt after the identity block, inside the cached prefix. `/status` lists loaded context files. Flat lookup only — no directory walking. | ZETA-23 |
| ZETA-26 | Skills loader (fills the ZETA-21 stub): `src/zeta/skills/*.md`, frontmatter = name + description + trigger keywords, body = prompt content. System prompt carries a name+description index; a built-in `skill` tool loads a body on demand. No per-skill tool mounting. | ZETA-24 |
| ZETA-27 | Session ergonomics: `/model` (swap model in place), `/compact` (force compaction now), `--resume` picker listing recent sessions with first-message preview. | ZETA-7, ZETA-19 |
| ZETA-28 | Native non-text tool content transport: send supported image blocks to Anthropic tool_result natively, use one metadata-rich text fallback for Codex and provider-limit cases, persist image blocks, and render bounded TUI placeholders. Replaces the ZETA-24 flatten fallback. | ZETA-24 |
| ZETA-29 | Full-screen alternate-buffer TUI parity round 2 (OpenCode chrome): transcript viewport, pinned composer/footer, tool-call cards with truncation affordance, one-line tool receipts, styled thought lines with duration, vertical rhythm, status-bar state, and theme consolidation. Todo widget + inline images out of scope. | ZETA-20 |
| ZETA-30 | User-configurable lifecycle hooks: load flat `~/.zeta/hooks.toml` at session start and run shell commands at session_start, user_prompt_submit, pre_tool, post_tool, and stop. Pre-tool hooks can deny with exit 2; other hook failures fail open with a dim notice. Hooks receive event JSON on stdin, inherit the environment plus `ZETA_SESSION_ID`, enforce bounded process-group timeouts, and appear in `/status`. Fake-provider runs disable hooks unless `ZETA_HOME` explicitly opts into an isolated home. | ZETA-19 |
| ZETA-31 | Checkpoints + `/fork`: `/checkpoint [label]` appends a durable `type="checkpoint"` chrome entry to the conversation chain; `/fork` with no args lists checkpoints (seq, label, age, next-message preview); `/fork <label|seq>` appends a `type="fork"` entry whose parent is the checkpoint entry, atomically switching the active branch — the abandoned branch stays in the JSONL untouched. Checkpoint/fork entries never reach provider context (filtered like warnings). Fork only at turn boundary (reject mid-turn); branching from before a compaction marker restores the uncompacted entries (marker is off-branch); abandoned-branch pending approvals must not leak (existing store invariant). Resume after fork lands on the forked branch via plain replay. TUI rebuilds the transcript from the new branch after fork. | ZETA-7, ZETA-19 |
| ZETA-32 | Composer attachments: reference local files in the composer (path autocomplete or explicit `@path` syntax), attached as content blocks on the user message — text files inline with size bound, images via the ZETA-28 native transport. | ZETA-28, ZETA-29 |
| ZETA-33 | Todo-list tool: harness-native task tracking tool (create/update/list) with a pinned TUI widget rendering the current list; persisted in session state. Lifts the ZETA-29 out-of-scope todo widget. | ZETA-13, ZETA-29 |
| ZETA-34 | Fetch pagination: `offset` parameter on the fetch tool slicing the extracted readable text (`body[offset:offset+limit]`); truncated results carry `full_size` + `next_offset` metadata and the tool description documents continuation, so long documents are readable end to end instead of dying at the registry 10,000-char output cap (Henry-hit live 2026-08-26). Notices prepend each page without consuming offset space. | ZETA-13 |
| ZETA-35 | Ctrl+V image paste: composer keybinding invoking the same clipboard-grab path as `/paste` (pending-chip semantics identical). Cmd+V is impossible in a terminal (emulator swallows it; image clipboard never traverses the pty) — Ctrl+V is the claude-code-precedent binding. When the clipboard holds text, Ctrl+V falls through to normal text paste behavior; only image content triggers attachment. | ZETA-32 |
| ZETA-36 | Inline image tokens (claude-code parity, Henry 2026-08-26): a paste inserts a compact `[Image #N]` token INTO the composer text at the cursor instead of transcript/footer chrome. Tokens map to staged images; submit resolves tokens in order to attachment blocks; backspacing a token away cancels that pending attachment; numbering resets per message. DELETE the "system · pending image: <path>" transcript notice and the "[pending attachment: ...]" footer line entirely. Sent-message transcript rendering shows the token inline where it sat in the text. | ZETA-35 |
| ZETA-37 | Sub-agent core (arc: sub-agents, Henry 2026-08-26): built-in `agent` tool spawning a bounded child AgentLoop — fresh ConversationStore under `sessions/<sid>/agents/<n>/`, parent's provider/model, parent's tool registry MINUS the `agent` tool (no nesting v1). Child runs to completion with a turn cap (default 25; loud error result on cap); final assistant text returns as the tool_result. Approvals surface through the parent's pending-approval machinery; parent abort propagates to the child; parent resume does NOT revive live children (canceled marker in both stores). Child JSONL persists for inspection. v1 rendering: one status line via the ZETA-17 streaming-tool-output path — the full TUI card is ZETA-38. | ZETA-13, ZETA-17 |
| ZETA-38 | Sub-agent TUI card: collapsible card for a running `agent` tool call — description line, live current-step line (from child stream), elapsed time, turn count; collapsed one-line receipt on completion (claude-code Task-card parity). Expandable to a bounded tail of child transcript. | ZETA-37, ZETA-29 |
| ZETA-40 | Fix inline-markdown spans breaking at commit boundaries (Henry-hit live 2026-08-27): a completed assistant line rendered `**single` + a dangling backtick literally while earlier bold on the SAME line rendered fine — an inline span whose closing marker arrives after the line/chunk commit renders raw and the completed-message markdown re-render (#56) does not repair it. Reproduce first with the observed shape (numbered list item, multiple bold spans, span opening near end of committed chunk), then fix so completed messages render all inline spans correctly regardless of stream-chunk or wrap boundaries. | ZETA-8 |
| ZETA-41 | Parallel sub-agent execution (Henry-hit live 2026-08-27): multiple `agent` tool calls in one assistant turn run sequentially even though the loop has a parallel dispatch path (loop.py `parallel_safe` runs) — NO tool in the repo opts in, so the machinery is dead code. Make the `agent` tool parallel-safe: remove the mutable-registry call context (`registry.active_tool_call` / `agent_runner` read it per-execution — concurrent children clobber it; pass the tool call explicitly through the execution path), make child-index allocation under `sessions/<sid>/agents/<n>/` race-safe, verify delegated approvals from multiple live children surface and resolve independently (keyed by `child_instance_id` — looks ready, prove it), propagate parent abort to ALL live children, and render multiple concurrent ZETA-38 cards with interleaved child streams. Also opt in the obviously-safe read-only tools (`read`, `list`, `fetch`) so the parallel path stops being agent-only. Regression tests: two-children-in-one-turn wall-clock/interleaving proof via fake backend, concurrent approval resolution, abort-with-two-live-children, card rendering with two live children. | ZETA-37, ZETA-38 |
| ZETA-42 | Resume replays the transcript (Henry-hit live 2026-08-27): `--resume <id>` opens a visually BLANK TUI even though the conversation store loads — the transcript-rebuild machinery exists (`checkpoints.py` `_rebuild_transcript()`, used after `/fork`) but the resume startup path never invokes it. Make resume render the prior conversation before the composer accepts input: user messages, completed assistant messages (via the ZETA-40 pure-final-text path), tool receipts, sub-agent receipts, checkpoint/fork chrome, compaction markers — same filtering rules as fork rebuild (warnings and provider-hidden entries stay hidden). Transcript starts scrolled to the bottom. Works in full-screen and inline modes. Regression tests: resumed render byte-identical to the post-fork rebuild of the same store; resume-blank bug pinned (fails on old code); long-session resume stays bounded (no per-entry provider calls). | ZETA-31, ZETA-40 |
| ZETA-43 | Failure resilience UX (arc: daily-driver polish, Henry 2026-08-27): provider death or network blip mid-turn must surface a visible error card with the failure reason — never a silent hang. Partial turn persists with durable canceled markers (existing store invariants); the composer draft is NEVER lost (including text typed while the turn streamed); one-key retry resubmits the failed turn from the same conversation state. Sub-agent children: a dead child errors its own card and returns a loud error tool_result — the parent session survives. Regression tests: scripted provider failure mid-stream (fake backend), network-error shapes, child-death isolation, draft survival, retry produces a well-formed next turn; forced-terminal throughout. | ZETA-29, ZETA-41 |
| ZETA-44 | Composer QoL (arc: daily-driver polish): cross-session prompt history — up-arrow walks prior prompts (this session then earlier sessions), ctrl+r incremental search; draft preserved across restart/resume (durable draft file per session, cleared on send); multi-line editing polish; sent-too-early undo scoped as abort-in-flight-turn + restore the message text into the composer. History lives under the zeta home dir, bounded size, never contains attachment bytes. Tests: history walk order, search, draft round-trip through restart, undo restores text and aborts cleanly. | ZETA-43 |
| ZETA-45 | Transcript navigation (arc: daily-driver polish): find-in-transcript with match highlight and next/prev keys; jump to previous/next user message; position indicator while scrolled up (line n/N or percent); paging stays smooth on long resumed sessions (no per-keystroke full re-render). Full-screen mode primary; inline mode exempt where the affordance has no meaning. Tests: search matching and traversal, jump targets, indicator accuracy, a large-session paging bound; forced-terminal. | ZETA-29, ZETA-42 |
| ZETA-46 | Observability UI (arc: daily-driver polish; lifts the ZETA-23/39 deferred observability half): richer /status — cache hit rate over the session (per-turn trend, not just cumulative), token spend by category (input/output/cache read/cache write) with a cost estimate from a small static price table, context-window fill gauge, compaction history (when, entries folded, tokens saved). Read-only over existing ZETA-23 usage counters — no new accounting paths. Tests: rendering against fake-usage sessions, price-table coverage of both providers' current models, gauge math at boundaries. | ZETA-23, ZETA-39 |
| ZETA-47 | Typed sub-agents (arc: sub-agent maturation, Henry 2026-08-27): optional `agent_type` argument on the `agent` tool selecting a built-in preset — own system-prompt preamble, tool subset (`explore` = read-only tools, no exec/bash; `plan` = read-only + todo; `general` = current behavior), and per-type turn cap. Preset registry with built-ins only (user-definable types deferred); unknown type returns a loud error tool_result, never a silent default. Child TUI cards and receipts show the type. Tool description documents the types so the model can pick. Tests: per-type tool-subset enforcement (a child of type explore attempting exec gets a registry error), turn caps per type, unknown-type error, card/receipt type display, default remains `general` with today's exact behavior. | ZETA-37, ZETA-41 |
| ZETA-48 | Background sub-agents (arc: sub-agent maturation; SEQUENCING: start after ZETA-43 merges — shared loop failure-path surface): `background: true` on the `agent` tool returns immediately with a handle receipt card; the child runs across turn boundaries; completion writes a durable notification entry rendered at the next turn boundary and surviving resume. Resume policy consistent with ZETA-37: a background child alive at session exit is NOT revived — its receipt renders canceled, never silent loss. Parent abort cancels live background children. Parallel-safe alongside foreground children. Tests: background completion notification, resume-after-exit canceled receipt, abort propagation, foreground+background mix. | ZETA-37, ZETA-41, ZETA-43 |
| ZETA-49 | One-level sub-agent nesting (arc: sub-agent maturation; lifts the ZETA-37 no-nesting bound): children may spawn grandchildren with a hard depth cap of 2 (grandchildren get a registry MINUS the agent tool) and a SHARED turn budget across the tree (parent-configured, default = child cap; a nested tree cannot multiply turns invisibly). Budget exhaustion yields loud error results up the tree. Cards show nesting depth. Tests: depth-cap enforcement, shared-budget accounting across siblings and generations, exhaustion error shape, abort through two levels. | ZETA-47, ZETA-48 |
| ZETA-50 | Todo tool write path is undiscoverable (Henry-hit live 2026-08-27): the schema declares `action` enum `["read"]` only and the description never mentions writing, while the handler supports writes via the undocumented shape items-present-without-action (`todo.py` `_todo` -> `set_todo_items`) — the model reads the contract and concludes the tool is read-only. Fix the contract: either add `"write"` to the action enum or drop `action` for documented items-presence semantics (pick the simpler shape); description documents both read and write in one line each; `status` gains its value enum in the schema (mirror `parse_todo_items`); governance test asserting every handler branch is representable in the declared schema (schema/implementation parity). | ZETA-33 |
| ZETA-121 | Image-capable `read` tool for PNG, JPEG, GIF, and WebP files, with native Anthropic tool results, Codex image injection, durable replay, and compact TUI receipts (arc: harness parity; lane 5 of 5). | — |
| ZETA-124 | Deliberate type scale + reading measure for the GUI (arc: GUI polish, lane 2 of 4): the run-UI reads as one uniform mono size where hierarchy is done almost entirely by weight — headers, sidebar rows, prose, hints, and metadata all sit at the appearance base with the same 600-weight promotion. Introduce a named role ladder in `theme.rs` — `title(base+2)`, `body(base)`, `label(base-1)`, `label_small(base-2)`, `label_micro(base-3)` — that rides the ZETA-111 base and floors at `MIN_LABEL_PX` so the picker's 11px→18px window stays legible. Every text call site (`main.rs`, `sidebar.rs`, `transcript_render.rs`, `session_management.rs`, `polish.rs`) routes through a named role: transcript prose = body, run-header session title + modal titles + sidebar "zeta" = title, sidebar session rows + branch rows = label, metadata (band-2 usage/model, session age, `current` chip, appearance range hint, modal field captions) = label_small, hints (composer step, sidebar hints, empty-transcript, drop-target hint) = label_small, state pill = label_small. Add a `prose_max_width(base) = base * 0.62 * 88 + 32` reading measure (the `+32` folds the row's 16px horizontal padding on each side into the cap so the TEXT area inside the padding lands on the promised ~88ch, not ~4ch shorter as it would if padding sat outside the cap) so the transcript's user/assistant/thinking rows cap at ~88ch of the base font — narrow enough to be scannable at 13px (~741px, 88ch text + 32px padding) and still comfortable at 18px (~1014px, still strictly under `TRANSCRIPT_MAX_WIDTH`) — while tool receipts and error blocks keep `TRANSCRIPT_MAX_WIDTH` for wide tool command lines and stack blocks. Fenced code blocks INSIDE an assistant markdown row ride the SAME prose cap as their surrounding prose (the cap sits on the row wrapper, not on per-block splitters); the wide cap only applies to tool receipts and error blocks, and `assistant_code_fence_rides_the_prose_cap` pins this accepted narrower shape so a future per-block splitter change lands with the review note updated in the same diff. Route the tool-row chevron icon size through `label_small` too so a chevron matches the row it sits on at every picker step. Guard rails: a scan-based `every_text_size_call_routes_through_a_named_theme_role` test rejects `.text_size(px(...))` outside the theme/tests; `role_scale_lands_on_an_ordered_ladder` pins the ladder; `prose_max_width_scales_with_the_appearance_picker` locks the measure into a scannable band; `every_role_clears_wcag_aa_against_canvas_on_every_theme` extends the ZETA-111 matrix to the new roles; `transcript_prose_column_caps_at_reading_measure_and_centers` (updated from the ZETA-107 wide-cap test) proves the prose column caps and centers at the narrower measure while `tool_rows_keep_the_wide_transcript_column` proves the split gate; `wedge_list_item_wraps_inside_the_prose_column` pins the exact ZETA-124 critique content (`(conversation.jsonl` / `meta.json)` inline chips + hanging-indent continuation) at the prose measure so the r0 orphan-glyph shape cannot re-manifest — the narrower measure changes the wrap point past the gpui-base list-row `overflow_hidden()` boundary where the orphan was landing. Screenshots: `docs/screenshots/ZETA-124-{before,after,11px,18px}.png` show the flat pre-scale look, the promoted hierarchy, and the role ladder tracking the 11/18 picker floors. Client-only: no RPC, protocol, or server changes; the ZETA-109 renderer-literal fence still passes because the new sizes ride tokens, not visible-text literals. | ZETA-111 |
| ZETA-127 | Native pixel-gutter wrap guard (arc: GUI polish, infra follow-up): extend the existing fake-server smoke driver with an explicitly invoked `ZETA_GUI_NATIVE_GUARDS=1` arm. Derive two display-safe window sizes, wait for native resize delivery, and assert each capture's achieved dimensions. Record achieved sizes in every matrix entry; warn and skip duplicate achieved viewports, then fail unless two distinct widths were rendered. Render the critique, adjacent numbered list, deeper nesting, and a genuinely over-budget long token at 11px, 13px, and 18px for each distinct viewport. Capture real native pixels and scan the band from the prose content-box edge to the scrollbar rail; two adjacent over-threshold non-background pixels fail with the shape, size, and x range. Keep the ZETA-124 recorder and formula tests as the headless inner layer, and document the native pixel arm as the outer layer. Recreate the round-3 evasion with `ZETA_GUI_NATIVE_GUARDS_FORCE_TEXT_WIDTH=1`; the guard must fail. Wire `make gui-native-guards`; macOS CI can clamp a requested window to the available display, so the guard derives requests below that limit and validates the rendered sizes. The restored macOS CI step is authoritative. Depends on ZETA-124. | ZETA-124 |
| ZETA-125 | Tool receipt redesign (arc: GUI polish, lane 3 of 5): the pre-fix critique showed eight consecutive `bash stdout:` rows with byte sizes right-aligned across a huge column — the receipts never named the command that ran, and the metadata was too far from what it described to read as attached. Every receipt now shows a stable EXCERPT of the tool call (bash/exec/shell → first line of `command`; read/write/edit/list → `path`; fetch/webfetch → `url`; other tools → first primitive arg, else the tool name alone), derived at `tool_entry()` construction from `tool_call.arguments` and stored on `TranscriptEntry::Tool::excerpt` (bounded at `EXCERPT_CHARS = 160` with a single-character ellipsis). The row's PRIMARY text becomes the excerpt at the state color (running=foreground, done=muted, failed=danger); the tool name demotes to a small semibold muted label. Size metadata sits DIRECTLY after the excerpt (laws-of-ux proximity) — flex_shrink_0 at label_small, not right-aligned across the whole column. A run of `TOOL_GROUP_MIN_LEN = 3` or more consecutive tool receipts collapses into ONE group summary row ("N tool calls" · previewed first `TOOL_GROUP_PREVIEW_MAX = 2` excerpts · total output bytes); interior rows paint a zero-height spacer so virtual-list index math stays 1:1 with `TranscriptEntry` indices without a projection layer. Group expansion is stored on `AppState::tool_group_expanded` keyed by the first receipt's `tool_call_id` (stable across a run that grows a new tail); `is_tool_group_expanded` forces every group in the LIVE turn expanded while `streaming` so live tool activity stays on screen without a click, and each group in the historical turns keeps whatever state the user set it to. Group summary rows are real tab stops (matches ZETA-108's a11y precedent) with a `FocusHandle` cached per group in `ZetaView::tool_group_focus`; Enter/Space toggles the group and remeasures the run's rows through the ZETA-107 ordered-edit view-sync. The accessible label carries the expansion state (`, collapsed` / `, expanded`) so keyboard-only users hear what changes on activation. The `row_text` model extends with `ToolRowText { tool_label, excerpt, metadata_label, hover_hint, tail_omitted_hint, body }` and a `ToolGroupRowText { count_label, total_label, preview_excerpts, aria_label }` variant plus a `ToolGroupHidden` marker; every visible group string is composed in `build_tool_group` so the ZETA-109 renderer-literal fence continues to pass unchanged. Tests: `zeta125_excerpts_route_by_kind_and_truncate` covers bash/read/write/edit/fetch/other + ellipsis truncation; `zeta125_metadata_paints_directly_after_the_excerpt` asserts the metadata's x-origin sits within 64px of the excerpt end (the pre-fix layout right-aligned it across the whole column and this test is that gap's grave); `zeta125_grouping_at_three_or_more_receipts` proves the 3+ threshold and pins the composed count/total/preview strings; `zeta125_group_toggles_on_mouse_and_keyboard` drives expansion through the same state seam the click AND key handlers dispatch through; `zeta125_streaming_forces_current_turn_groups_expanded` pins the streaming override; `zeta125_receipt_layout_holds_at_11px_and_18px` runs the layout across the appearance picker's floor and ceiling. No RPC, protocol, or server changes — the underlying entries, ordering, and fork anchors stay intact; grouping is presentation only. Screenshots: `docs/screenshots/ZETA-125-{before,after,after-grouped}.png`. | ZETA-124 |
| ZETA-120 | `/init` slash command submits a repository-exploration prompt that creates or improves root `AGENTS.md`, with nested `AGENTS.md` guidance for folder-specific rules (arc: harness parity; lane 4 of 5). | — |
| ZETA-126 | Polish interaction feel (arc: GUI polish, lane 4): the structural lanes fixed what the GUI LOOKS like; this lane fixes how it FEELS. Every clickable list-row surface now paints a two-step feedback staircase — hover ↦ `theme.list_hover` / `theme.muted` rest wash, pressed ↦ `theme.list_active` one tint step stronger — attached via `.hover(...).active(...)` on the sidebar session row, sidebar branch row, tool receipt row, and tool group summary row. `.active()` layers on top of `.hover()` and applies while gpui's interactivity holds `pressed_button = Some(Left)`, so a held click paints the pressed fill for the full duration of the hold WITHOUT any animation timing. Sidebar rows dispatch on `on_mouse_down` (session activation is worth the snap), so the pressed style there is transient; tool receipts + group headers dispatch via `.on_click` (mouse-UP), so the pressed fill paints for the full hold — both surfaces share the same declarative `.active(list_active)` refinement so mutations reject uniformly across kinds. Motion — the drop-target overlay enters via a 120ms `ease_out_quint` opacity fade (`with_animation`) so the layer no longer pops in the instant a drag crosses the composer bounds; `with_animation` respects `App::reduce_motion`, so under the OS reduce-motion setting the overlay renders at its end state with no scheduled frames and CI determinism holds. Deliberately skipped motion (documented for restraint, not oversight): group / receipt expand-collapse (opacity fade over a resizing row would race the ZETA-107 virtual-list anchor and risks visible jitter); modal open/close (every existing settings paint probe checks EXACT painted colors via `painted_quads` background match and element opacity multiplies those, so a fade would break the ZETA-128 round-6 mask, sidebar-token, focus-ring, and scroll-cue tests); scroll-cue fade (mask is a snapped alignment layer, not an animated one — adding opacity fights the snap); hover-state easing (gpui `.hover()` is a style refinement, not a transition primitive — an ease would require a shadow element with `with_animation` per hovered site). Restraint mandate held: no bounce, no long fades, every animation interruptible by user input. Mandatory deferred ZETA-123 conversion: the `sidebar_rows_are_tab_focusable_paint_a_focus_cursor_and_activate_on_enter_and_space` test used `window.focus(&handle)` to jam focus onto a row — which succeeds even for handles that never registered as tab stops. Converted to dispatch REAL `simulate_keystrokes("tab")` / `simulate_keystrokes("shift-tab")` events: the final `tab_to` helper seeds focus on a known control, then walks genuine Tab keystrokes with a repaint per step, proving BOTH reachability (Tab reaches the row through Root's binding) AND the row-focus trigger's paint (accent quad on the following frame); a real Shift-Tab back-step extends the guard to the reverse cycle. Landing this honestly forced two production fixes: sidebar rows registered as real tab stops, and the composer's Tab keybinding changed from IndentInline to focus traversal (block indent moved to cmd-]/cmd-[), removing an app-wide keyboard trap — the UX change is called out prominently in PR #170's body. Tests: `tool_receipt_and_group_row_paint_a_pressed_state_between_mouse_down_and_up` in `tests.rs` proves the pressed-state contract with a paint-count probe on `theme.list_active` inside the row bounds — 0 hits at rest, ≥1 hits after `simulate_mouse_down`, 0 hits after `simulate_mouse_up` — and the tool receipt + tool group header share the same code path so the probe covers both surfaces. Scroll feel: the ZETA-107 ordered-edit view-sync anchor and virtual-list remeasure path stay untouched (verified, not reworked); focus-ring transition already sits on gpui's synchronous focus dispatch so no lag frame exists to smooth. Scope: `gui/src/main.rs`, `gui/src/sidebar.rs`, `gui/src/tool_receipts.rs`, `gui/src/tests.rs`. Client-only: no RPC, protocol, or server changes; the ZETA-109 renderer-literal fence still passes because every new fill token routes through `cx.theme()`. Screenshots: `docs/screenshots/ZETA-126-after.png` (sidebar hover/pressed staircase on a switchable session row) and `ZETA-126-after-composer.png` (composer after the Tab-UX change). | ZETA-123, ZETA-124, ZETA-125, ZETA-128 |
| ZETA-130 | Slash commands in the GUI (arc: GUI polish 2, lane 2 of 6): before this ticket the desktop app had no slash surface at all — typing `/model` sent that literal to the model as chat, and the model answered a hallucinated "current model" while the status bar said the true one. Protocol 1.1 grows two RPCs — `slash_list` and `slash_run` — that reuse the shared `SlashCommandRegistry` in `src/zeta/core/slash.py` end-to-end (no second registry in `gui/`). `slash_list` returns every command the shared dispatcher knows about with `name`, `description`, `kind` (`builtin` / `macro-prompt` / `macro-exec` / `skill` / `mcp-prompt`), `source`, and two flags — `client_only` for commands that need a client-side surface (picker, workspace mutation, transcript navigation) and `unavailable` for commands the RPC deliberately does not drive. `slash_run` dispatches one invocation through the shared registry backed by a `ServerSlashSession` shim over `ServerRuntime`; the reply is discriminated by `kind`: `output` (server-composed notice), `model_input` (resolved macro text the GUI sends via the normal `send` path), `client_only` (echo of the name for the GUI to dispatch locally), `unknown`, or `error`. Server-side scope floor covers `/status`, `/compact`, `/model` (routed through the existing `set_settings` apply path — no new picker RPC), `/init`, `/help`, prompt macros from `.zeta/commands/*.md`, and skills; exec macros and every command in the `client_only` set report themselves as client-only rather than half-executing here because the approval loop and TUI-only mutations do not have server-side seams (documented in the PR body and the `slash_commands.py` module doc). Legacy 1.0 servers reject both requests with `-32601`; the GUI mirrors the existing `session_extensions` gate so old-server + new-GUI hides the menu entirely and no `/`-prefixed value reaches the model as chat. GUI: `gui/src/client.rs` grows `slash_list` / `slash_run` typed methods plus `SlashCommandInfo` / `SlashRunResult`; `gui/src/worker.rs` adds `CommandMessage::SlashList` / `SlashRun` plus `WorkerMessage::SlashCommands` / `SlashResult`; a small `gui/src/slash_menu.rs` module owns the menu state (open flag, filter, selection, notice) and its pure helpers (`leading_slash_token`, `compose_selection`) so the composer never carries wire logic; `main.rs` renders the menu as a compact card ABOVE the composer input row (styled with the ZETA-124 type roles and ZETA-128 sidebar / border tokens — no hardcoded px sizes, no literal renderer strings). Interaction: typing `/` at the start of the composer opens the menu and it stays open while the composer's first token starts with `/`; the filter narrows as you type; `up` / `down` (bound as a capture-phase `slash_menu_key` root handler that preventDefaults before the Textarea gets the arrows) move the highlight, `Enter` populates the composer with `/<name> ` (preserving any argument tail already typed) and keeps the menu open for the argument hint, `Esc` dismisses, a left-mouse-down on a row selects. Send-time invariant: `send_composer` routes every value whose first non-space char is `/` (and is not the `//` escape) through `slash_run` — never through `send` — so `/foo bar` cannot leak to the model as chat text even when the menu has been dismissed. Client-only surfaces the GUI wires directly in this ticket: `/new` (`new_session`) and `/theme` (`open_settings`); every other client-only entry paints a bounded "runs client-side" notice inside the menu until its native surface lands as a followup. Slash output renders as a quiet strip ATTACHED to the composer (a `slash-output` element sitting above the input row on `muted_foreground`, or `danger` when `kind: error`) — full transcript-row integration is deferred so this PR does not touch the row-text model, ZETA-109 fence, or transcript render dispatch. Menu render + input handling live in `main.rs::render_slash_menu` / `render_slash_output` / `select_slash_menu` / `slash_menu_key`; catalog refresh follows the session lifecycle (queued once per session after `WorkerMessage::Session`, reset on session switch). Tests: server side pins `slash_list` classification for built-ins / macros / skills, `slash_run` scope floor for `/status`, `/model`, `/model faster`, `/compact`, a prompt macro from `.zeta/commands`, a client-only builtin, an unknown command, a shape error, legacy 1.0 rejection with `-32601`, and busy-turn rejection with `-32004`. GUI side pins `slash_menu::leading_slash_token`, `sync` (opens on `/`, dismisses on non-slash, saturates selection, clamps filter), and `compose_selection` (preserves argument tail). Screenshots: `docs/screenshots/ZETA-130-{after,unknown}.png` capture the menu open over the composer and the unknown-command notice. Exclusions to declare in the PR body rather than half-shipping: exec macros over the RPC (no server-side tool-approval loop), inline-shell resolution inside prompt macros (needs approval flow), and full transcript-row integration for `output` / `error` (deferred until row_text + transcript_render gain a slash-receipt variant). | ZETA-124 |
| ZETA-128 | Restructure the Settings surface (arc: GUI polish, lane 5): the Session-settings modal is three grouped sections — Model, Behavior, Appearance — each with a body-tier semibold heading and a hairline divider rule. Every labeled row flows through ONE anatomy: `settings_row(selector, label, description, control, cx)` — a token-derived label column on the left, a right-aligned control slot, and an optional muted-foreground caption below routed through `label_small`. The label column widens with the picker base (`settings_label_column(base) = round(base * 10.8)` — 119px at 11px, 140px at 13px, 194px at 18px) so long labels ("Approval mode") do not wrap into a stacked block at the 18px picker MAX. The pre-round-1 stacked "caption above buttons" shape retires; the pre-round-2 button walls in Appearance retire too — Theme and Font collapse into ONE focusable cycler each (compact single-value select — clicking advances one step through `ThemeId::ALL` / `FONT_FAMILIES` and wraps at the ends), cutting Appearance from nine tab stops to two so every focus ring paints inside the 760px viewport at 18px. The font-size row keeps the shrink/value/grow stepper primitives without a "range 11-18px" caption — the disabled `−` at MIN and `+` at MAX carry the picker window. Full keyboard traversal inside the modal: Tab / Shift-Tab walk every control (approval buttons, theme cycler, font cycler, stepper, Close, Apply); Escape closes and returns focus to whoever opened the modal — the focus handle is captured inside `apply_worker_message::Settings` (the moment settings actually opens) so callers on other surfaces (`sidebar.rs`, `transcript_render.rs`) do NOT need to thread a `Window` through `open_settings`. Safety net for the sections that overflow at 18px: the sections wrapper carries `overflow_y_scroll` + `.track_scroll(&settings_sections_scroll)`; each section container carries `.track_focus(&handle)` with a lazily-allocated persistent handle (same pattern as `sidebar_row_focus`) so the render pass checks `contains_focused(window, cx)` on each section and calls `scroll_to_item(section_ix)` before the frame paints — the focused control's parent section swings back into view. Modal chrome: the `esc` hint moves off `theme::palette::text_faint()` (2.92-4.03:1 on the panel across the five themes — WCAG AA needs 4.5:1) and onto `cx.theme().muted_foreground`, which clears AA on every theme; row descriptions ride the same token. Panel height is `min(shelf, SETTINGS_PANEL_MAX_HEIGHT)` where `shelf = viewport * (1 - MODAL_TOP_FRACTION) - MODAL_PADDING_X`: short viewports pack the panel tight against the shelf so `flex_1 + min_h_0` on the sections wrapper has a definite parent; tall viewports (1200px+) cap at 560px so the flat modal keeps the wiki-modal silhouette instead of stretching to page height. Close / Apply stay at the panel bottom inside the viewport at every picker base. Modal focus trap: the overlay wraps its content in `.focus_trap("settings-modal", &self.settings_focus)` (from `gpui_kit::base`), which registers the panel with the focus-trap manager; Root's `Tab` / `TabPrev` actions consult the manager and cycle focus back inside the modal whenever a step would escape to a background tab stop (composer, sidebar). Model rows carry per-index persistent focus handles (`settings_model_row_focus`, same lazy `sidebar_row_focus` pattern); each Button is wrapped in a `div().track_focus(&handle)`, and the render pass scrolls the FOCUSED row into view (falls back to the SELECTED row when nothing is focused) so Tab-through-rows never leaves the ring outside the model list's clipped viewport. Theme tokens: `settings_label_column(base)`, `SETTINGS_SECTION_GAP` (10), `SETTINGS_ROW_GAP` (6), `SETTINGS_ROW_DESCRIPTION_GAP` (2), `SETTINGS_MODEL_LIST_MAX_HEIGHT` (160), `SETTINGS_PANEL_MAX_HEIGHT` (560). Behavior is frozen — Model selection, approval mode, theme/font/size options, persistence, and fallbacks stay identical (`prefs::commit` fires on every cycler click with the same values the pre-round-1 button wall did). New tests in a dedicated ZETA-128 region at the end of `tests.rs`: three grouped sections render with their `-heading` nodes; every labeled row has label bounds left of control bounds on the same baseline; the stepper drops the range caption and keeps its three primitives; Escape closes and restores focus to the captured invoker; Tab moves focus off the overlay onto a modal tab-stop control; the panel paints on the sidebar token across all five themes; Close/Apply stay inside the 760px viewport at BOTH 11px and 18px picker bases; the theme + font cyclers render inside their row control slots and the pre-round-2 button-wall selectors (`theme-row-*`, `font-row-*`, `settings-*-segmented`) are gone; every row description slot renders inside its row container; the label column widens strictly with the base font size (11→18); the `esc` hint paints on `muted_foreground` and its foreground-vs-panel contrast clears WCAG AA on every theme (mirrored by a `modal_hint_and_row_description_contrast_clears_wcag_aa_on_panel` test in `theme.rs`); at 18px every named tab stop (approval buttons, cyclers, stepper primitives, Close, Apply) sits strictly inside the viewport. Scope: `gui/src/main.rs` + `gui/src/theme.rs` + `gui/src/tests.rs` + `gui/src/session_management.rs` (one-line pass-through of `cx` to the updated `modal_title(title, cx)` signature). `gui/src/sidebar.rs` and `gui/src/transcript_render.rs` are untouched — the focus-capture centralisation makes their round-1 one-line window-pass-through unnecessary. Client-only: no RPC, protocol, or server changes; the ZETA-109 renderer-literal fence still passes because every new caption routes through the ZETA-124 label roles and theme tokens. Round-5 adds a bottom scroll cue on the sections wrapper: the wrapper's clip edge cannot cheaply snap to a row boundary (mixed row heights, no per-child measured heights during layout), so the panel paints an opaque `sidebar`-token overlay at the wrapper's bottom edge with a 1px `border` top-line — the "content continues below" cue Henry asked for. The overlay's height is `settings_scroll_cue_height(base) = max(body, MODAL_BUTTON_HEIGHT) + SETTINGS_ROW_DESCRIPTION_GAP + ceil(label_small * 1.75) + SETTINGS_ROW_GAP` (56px at 11px, 59px at 13px, 68px at 18px MAX), where the `MODAL_BUTTON_HEIGHT` floor covers the Ghost/compact button that owns the row header and the `* 1.75` scale turns the label-small FONT size into its RENDERED line-box height (gpui adds ~55% descender padding, so a 17px font actually renders at 26px — the shape the round-6 CI caught) so a caption is never sliced by the mask top edge. A trailing spacer of the same height reserves blank room after the last section so a scrolled-to-end user never sees the mask paint over real content. Round-5 also lands the FINAL focus registration: `settings_focus` is registered as a real `.track_focus()` tab-stop so a Tab-cycle from Apply wraps back to the modal anchor instead of escaping to a background surface; `settings_focus_is_a_focus_trap_anchor_the_cycle_can_reach` in `tests.rs` pins the wrap by dispatching a real `TabPrev` action and asserting focus lands on `settings_focus`. Screenshots: `docs/screenshots/ZETA-128-{before,after,after-18px}.png` — `after` (13px shipped default) and `after-18px` (18px picker MAX) both recaptured round 6 to include the scroll cue and the registered focus anchor; `before` remains the ZETA-124-era shape for the review diff. | ZETA-124 |
| ZETA-129 | Fix inline-code chip glyph loss + wrap-boundary collision (arc: GUI polish 2, lane 1 of 6): the 2026-09-16 audit found two classes of code-span corruption in every session. A1 — backtick spans of exactly 9 characters lose their last glyph (`task_kill` painted as `task_kil`, `websearch` as `websearc`, `thumbnail` as `thumbnai`); an 8- or 10-char span renders clean. A2 — a code chip pushed to a wrap boundary paints its overflow glyphs onto the next line at a stale x-position, over existing prose (`gpui::image` painted `gpui::im` with a bare `g` on the row below; `preview` painted `pr(g)eview` where the previous chip's last glyph landed inside it). Both bugs live in `gpui-base` v0.6.1 `InlineFlow::prepaint`: it hands the inner `StyledText` an available width of `Definite(fragment_size.width - INLINE_CODE_PADDING * 2)` — exactly `shape_line.width()` for the chip — which threads through `StyledText::layout` → `shape_text` → `line_layout_cache.layout_wrapped_line` → `compute_wrap_boundaries`. Real macOS CoreText (`gpui-pre-macos::layout_line` applies `font_size.next_up()` on the first font-run to break ligatures, `typographic_bounds.width` rounds independently of per-glyph advances) drifts `unwrapped_layout.width` a sub-pixel above the wrap_width passed in; `compute_wrap_boundaries` inserts a boundary the moment `width > wrap_width`, and for a bare identifier with no space `last_candidate_ix` stays `None` so the boundary lands ON the last glyph. That glyph paints one `line_height` below the fragment origin — invisible inside a chip that sits on its own row (A1) or on top of the next prose line when the outer flow already placed the chip on line 2 (A2). Length 9 is the exact drift threshold on the shipped 13px × 0.875 mono metrics; 8 and 10 both sit comfortably clear. Fix: vendor `gpui-base` 0.6.1 into `gui/vendor/gpui-base/`, byte-identical to the crates.io release except TWO authorised edits — (1) `InlineFlow::prepaint` swaps `AvailableSpace::Definite(fragment_size.width - padding * 2.)` for `AvailableSpace::MaxContent` on the width axis (env-toggleable via `ZETA_GUI_INLINE_FLOW_DEFINITE=1` for the poison-canary mutation arm), and (2) a `#[cfg(any(test, feature = "test-support"))]`-gated `zeta129_wrap_recorder` module plus a probe inside `InlineFlow::prepaint` that pushes each inner text fragment's `(text, font_size, wrap_boundaries)` sample using the SAME `wrap_width` the actual `prepaint_as_root` call receives. Release builds compile both the recorder and its probe out entirely. `gui/Cargo.toml` gains `[patch.crates-io.gpui-base]` pointing at the vendored copy so `gpui-kit`'s transitive use of `gpui-base` picks the fix up too. The outer flow (`layout_flow` + `push_text_wrap_fragments`) already splits over-wide code spans at word and grapheme boundaries and hands each `Inline` a portion that fits its fragment — inner re-wrap is redundant. Non-code text fragments already saw `Definite(shape_line.width())` (no padding); switching them to `MaxContent` is behavior-preserving because a text fragment's shape and available always matched. The vendor fork is temporary: `gui/vendor/README.md` names the exact diff and the unfork condition (a released `gpui-kit` whose `gpui-base` carries the equivalent fix), and the deferred / open follow-ups section carries the tracking entry. Guards land in the same PR: the `NATIVE_GUARD_SHAPES` matrix in `gui/src/smoke.rs` gains a `code_ladder` shape rendering backtick spans of length 1..=16 in a bullet list AND a `code_wide_token` shape rendering a 192-char backticked identifier wider than every tested column — the outer flow's `push_text_wrap_fragments` grapheme-splits the wide token; the existing pixel-gutter scan asserts no fragment paints past `content_right`. The real mutation-killer is `scan_inline_flow_recorder`, invoked after every native render inside `run_native_wrap_guards`: it reads `gpui_kit::base::zeta129_wrap_recorder::samples()` and panics on any non-zero wrap-boundary count. Under the fix (`MaxContent` axis) real CoreText metrics record zero boundaries on every fragment; under `ZETA_GUI_INLINE_FLOW_DEFINITE=1` the reinstated Definite axis re-enters `compute_wrap_boundaries` and CoreText drift trips the recorder — final CI trip evidence on `1b705b4`: shape=`wedge` size=13px sample text=`meta.json` wrap_boundaries=1 (the exact audit repro). A headless `zeta129_inline_code_chip_ladder_structure` test in `gui/src/tests.rs` inspects background quads only — it is NOT the mutation-killer (phantom-glyph paints are text sprites, not quads); it stays as a chip-structure regression cover so a future rework of the chip background paint still surfaces. Verification: CI's `gui-native-guards` job runs the extended matrix at all viewports and font sizes; `gui-native-guards-inline-flow-mutation` invokes the same driver with `ZETA_GUI_INLINE_FLOW_DEFINITE=1` and inverts the exit code — the recorder scan MUST trip. Screenshots: `docs/screenshots/ZETA-129-{before,after}.png` show the length-9 ladder corrupt on `d21ab5a` and clean under the patched crate. Client-only; no RPC, protocol, or server changes; the ZETA-109 renderer-literal fence is untouched. | ZETA-127 |
| ZETA-131 | Approval UX: visible selection, allow indicator, per-tool memory (arc: GUI polish 2, lane 3 of 6). The 2026-09-16 audit called out three high-leverage misses in the approval surface. C1 — the `ask/allow/deny` segmented control paints `Button::ghost().selected(true)` for the selected mode, which sits on `secondary_active` and is invisible on every shipped theme; users cannot tell which mode is on. C2 — nothing in the main UI ever shows the agent is running in `allow` (auto-approve); the modal Apply just closes silently and three tools ran unprompted with no warning anywhere. B3 — approvals have no memory: `Enter` approves one call, `Esc` denies one; the only escape from per-tool keypresses is the global `allow` toggle, and it is all-or-nothing. Fix stack: (C1) the segmented control now paints the SELECTED segment with the `.primary()` variant and leaves the other two as `.ghost()`, so exactly ONE segment carries the filled accent fill in every theme — `approval_mode_segmented_paints_a_filled_selected_state` pins the exactly-one-primary-fill invariant and reverifies after a click moves the selection. (C2) `SessionView` gains an `applied_mode` field that mirrors the server-applied approval mode from `WorkerMessage::Settings` and `WorkerMessage::SettingsApplied`; `render_run_header` reads `applied_mode` (NOT the modal's draft `selected_mode` — a mid-Settings selection must not spoof the header) and, whenever it equals `allow`, paints a `header-auto-approve` chip in the metadata cluster with a `warning_tint()` background, a `warning()`-colored dot, and the literal label `auto-approve` (text plus color, never color alone — audit B7). The chip is `flex_shrink_0` so it never collapses; `header_paints_auto_approve_indicator_only_when_allow_mode_is_applied` proves the ask/deny cases stay clear, the allow case paints tint + dot, a draft-only `selected_mode == allow` with `applied_mode == deny` does NOT paint the chip, and a fresh `Settings` snapshot from reconnect restores the chip. (B3) The `approve` and `deny` RPCs accept an optional `scope` param: `"once"` (default, legacy shape) or `"always_tool"` (only valid on `approve` — `-32602` on `deny`). Server-side, `always_tool` on the resolve path adds the pending tool's name (a bare `ApprovalRule`) to the session's `always_allow` set, so every subsequent call to that tool in the SAME session auto-approves via the existing `ApprovalPolicy.decide` path. `test_approval_scope_always_tool_records_session_policy_and_skips_next_turn` proves the session policy mutation AND the next-turn auto-approve (same-tool call runs without an `approval_request`, different-tool call still asks); `test_approval_without_scope_keeps_legacy_shape` pins the pre-ZETA-131 wire response for legacy clients; `test_approval_scope_rejects_invalid_values` proves the two guardrails. Wire shape carries `scope` back in the response only when the caller passed a non-default value — a pre-ZETA-131 client keeps its exact old shape. GUI-side, the client's `approve_always_tool(request_id)` wraps the `scope: "always_tool"` param; a new `CommandMessage::ApproveAlwaysTool(String)` variant reuses the existing approve pipeline in `worker.rs` (single-flight guard, status re-sync, same reject path). The approval dialog gains an `approval-always` button between the arguments and the footer hint reading `Always allow <tool> (this session)`, plus a bare `a` keybinding routed through `control_key` while the dialog is open. Both paths call `decide_always_tool`, which respects the same `approval_pending` debounce as `decide`. Dialog hint updates to `Enter approves · A always allows · Esc denies` so the keyboard shortcut is discoverable. `approval_dialog_dispatches_always_allow_via_click` and `approval_dialog_dispatches_always_allow_via_keyboard` verify BOTH activation paths dispatch exactly one `ApproveAlwaysTool` command carrying the request id, and no duplicate fires until the status reset. Product decisions locked at orchestrator level (deferred to a later arc — noted here): session-scoped memory, per-tool-NAME granularity; per-command / per-directory scopes and cross-session durability are OUT of scope. Scope: `src/zeta/server/server.py` (RPC + response shape), `docs/serve-protocol.md`, `tests/test_server.py`, `gui/src/theme.rs` (`warning_tint()` helper + palette accessor), `gui/src/session.rs` (`applied_mode` field), `gui/src/client.rs` (`approve_always_tool` + `decision` refactor), `gui/src/worker.rs` (`ApproveAlwaysTool` variant + dispatch), `gui/src/main.rs` (segmented C1, header chip C2, dialog + key B3, state sync), `gui/src/tests.rs`. Screenshots: `docs/screenshots/ZETA-131-{mode-selected,allow-indicator,approval-memory}.png`. | ZETA-124, ZETA-128 |
| ZETA-134 | Sidebar state correctness + small-fix batch (arc: GUI polish 2, lane 6 of 6): six audit findings converge on one PR because they all touch adjacent surfaces (sidebar rows, session server, composer footer, expanded receipts). A4 stops the server from leaking `"(no user message)"` into the sidebar preview after a reconnect — `SessionManager.list_session_previews` now emits an empty string, and every client's existing empty-preview mapping (GUI `session_label` → `New conversation`, TUI picker row → `(no user message)`, `zeta session list` CLI → `(no user message)` at the render site) takes over without string-matching the server text. A5 stops read-only session selection from bumping `updated_at`: `persist_skill_catalog` and `persist_agent_catalog` migrate their snapshot without a touch (they are one-time upgrades, not user activity); `record_budget` grows a `touch: bool = True` gate so `compose_runtime`'s resume-time budget reconciliation passes `touch=False`; and `persist_context_snapshot` only touches on the explicit `--system-prompt` / `--append-system-prompt` override path — automatic legacy hydration fills the snapshot in place without bumping the timestamp (r2 finding: a probe had shown a `2020-01-01` timestamp advancing to now on plain resume, which could reorder the sidebar). Callers that reflect real user intent (bootstrap's `on_budget_change` slash-command wiring, an operator override) keep the touching default. The sidebar sort now stays truthful across selection — the pre-fix "5m, 20m, now" ordering the audit reproduced can't recur. A6 aligns the sidebar's selected-row background with the active-dot indicator after Cmd-N: `apply_worker_message::Session` calls `retarget_sidebar_focus_on_session_change`, which reads the previous active session's row focus handle from `sidebar_row_focus` and, if it currently holds keyboard focus, moves focus to the composer. Composer focus is deliberate — the user just created (or picked) a session and is about to type; the pattern also matches the mouse-click activation path (GPUI's default focus move on click), so keyboard Cmd-N converges on the same end-state. A3 dedupes the tool-row label at the row_text seam by typing the missing-argument state: `TranscriptEntry::Tool::excerpt` is now `Option<String>` (the fallback that used to return the tool name is now `None`) and `ToolRowText::excerpt` carries `Option<&str>`; the excerpt element always paints (empty string for the missing-argument state) so the row's layout, debug selector, and state-color recorder stay stable, while an argument-less `tool_start` visually reads as `[▶] read` alone and a legitimate `read` file path (or a `bash` command literally named `bash`) surfaces its own text — the pre-r2 string-equality dedupe collapsed those real values into the label. C7 disables the Send button when the composer has no text and no valid attachments: `render_composer` computes `composer_has_content = !value.trim().is_empty() || any(valid attachment)` and Send now paints its disabled outline (already in place for the not-can_send case) whenever `can_send && composer_has_content` is false. Attach and drop-target stay live on the broader `can_send` gate so a user can drop an image into an empty composer to start a message. D6 reshapes the bash tool's expanded receipt: `state::reshape_bash_content` reads `stdout`/`stderr`/`exit_code` from `structured_content` and rebuilds the tail — omit the `stdout:` section when stdout is empty, omit the `stderr:` label when stderr is empty, append `exit: N`. Legacy servers without structured_content fall through untouched so the receipt never blanks out. Tests: `test_session_preview_with_no_user_message_returns_empty_string` (A4), `test_resume_does_not_bump_updated_at`, `test_legacy_resume_hydrates_without_bumping_updated_at`, and `test_first_legacy_resume_with_explicit_prompt_bumps_updated_at` (A5) in `tests/test_server.py` / `tests/test_session.py` — the last one pins the r3 finding that the very first legacy resume with an explicit `--system-prompt` must bump `updated_at` (the previous snapshot-presence gate silently persisted without bumping); `test_session_list_shows_placeholder_for_empty_preview` in `tests/test_session_lifecycle.py` covering the CLI empty-state label, `tool_row_uses_option_none_for_missing_argument_state` (A3, drives real `ToolStart` events for the hard-name cases — `read` reading a file named `read`, `bash` running `bash script.sh`, and missing arguments for both — then asserts stored excerpts, the painted excerpt bounds, and the chevron color sample recorded by `render_log`), `cmd_n_paints_a_single_current_row_and_moves_focus_to_the_composer` (A6, drives the real Cmd-N keystroke, then counts `theme.primary` painted quads inside the sidebar column split by height to prove exactly one focus row fill + one dot before Cmd-N on the same row and zero row fills + one dot on the new active row after), `send_button_disables_when_the_composer_is_empty` (C7), and `bash_expanded_tail_omits_empty_stderr_and_shows_exit_code` (D6) in `gui/src/tests.rs`. `tool_state_paints_by_color_alone_and_expanded_body_borders_by_error` also seeds a real `bash` command so the running-state paint recorder covers a non-empty excerpt. The single-selection invariant is pinned by the A6 gpui paint test (dot painted, no stale focus on the previous row, composer holds focus); the reshaped bash tail is pinned by the D6 unit test. Screenshots: `docs/screenshots/ZETA-134-after.png` (sidebar order after Cmd-N — the new session lands at the top of the list carrying the single-selection dot, previous rows keep their `updated_at` position) and `docs/screenshots/ZETA-134-receipt-after.png` (expanded receipt with the reshaped bash tail — empty `stderr:` label dropped, `exit: N` appended). Client + server changes; the ZETA-109 renderer-literal fence still passes (no new visible-text literals in renderer bodies), and ZETA-124 type roles are respected throughout. | ZETA-125, ZETA-128 |
| ZETA-51 | Main-red hotfix: loop.py over the module cap (found 2026-08-28 by the ZETA-50 lane): ZETA-43 (#72) and ZETA-47 (#73) each merged green independently, but their combined loop.py additions total 1,240 lines against the 1,200 cap — `test_module_limits` fails on main, blocking every lane. Extract a cohesive seam from `src/zeta/loop.py` into a focused module (per the repo's core/ layout; candidates: the parallel-dispatch batching block or the failure/retry plumbing) — PURE MOVE, zero behavior change, full suite green both envs. Process followup noted in the row: sequential same-day merges need a post-merge combined-suite check since the repo has no CI. | ZETA-43, ZETA-47 |
| ZETA-135 | Restyle GUI transcript to the wiki session-view look (arc: GUI polish 2, lane 7). Henry's 2026-09-16 direct request against the Wiki-app session view (`~/me/fun/wiki/vault/zeta/assets/wiki-look-reference-2026-09-16.png`): flat single-column document flow, one-line tool receipts with kind glyphs, bordered inset panels for expanded receipts, side-by-side diff panes for edit receipts with a narrow-viewport stacked fallback, a peak-end turn footer, and quiet composer chrome. The pre-ZETA-135 transcript already carried the flat document flow (1024px column cap, chevron-first receipts, muted state colors), so this ticket lands the FIVE remaining wiki-look traits at the shipped fence + a11y invariants — trait 1 kind glyph, trait 2 expanded-receipt inset panel, trait 3 diff card, trait 5 turn footer, trait 6 composer chrome. Trait 1 (kind glyph): every receipt now paints a leading `$` / `←` / `↗` / `⌕` / `⚙` glyph before the tool label so a transcript scan reads the row's KIND before its identity. The `row_text::ToolKind` classifier splits the edit family into `Edit` (read / list — no old/new pair) and `EditDiff` (edit / write / str_replace / str_replace_editor / multi_edit — carries the pair), and every downstream site — glyph, excerpt-argument keys, and diff-card gate — reads the CLASSIFIED variant directly (`is_diff_capable(self)` matches `EditDiff` only). A new alias added to `classify` alone lights up (or stays out of) all three sites without a second name match; the round-2 finding was that a name-only glyph mapping disagreed with the diff-card gate (`str_replace` classified as Edit for diff extraction but Generic for the glyph, `websearch` had no Search glyph), and the round-3 finding was that the diff-card gate still ran a raw-name match beside the classifier so a `classify`-only alias could get edit chrome without a diff card. The variant split closes both: `state::extract_edit_data` reads `ToolKind::classify(name).is_diff_capable()` on the classified value, so the classifier is now the SOLE fork. Trait 2 (expanded receipt = inset panel): the pre-fix expanded body drew a left indent rail (`pl_2 + border_l 1px + bg-sidebar`), which matches the collapsed receipt's rail rhythm but does NOT match the wiki look's fully-boxed inset panel. `render_tool_row` now paints the expanded body as a bordered container with `border_1()` on all four sides, a subtle `panel_border` fill from the theme sidebar token, and a top header row that shows what ran (file path for read/edit/write, command for bash, tool name for MCP) at the foreground tier with a dim border-bottom separator. The header row is fed from a new `ToolRowText::panel_header: Option<&'a str>` field populated by `row_text::build` as `Some(&excerpt)` when the row is expanded AND the tool call carried a nameable argument; an argument-less `tool_start` still stores `None` so the receipt opens without a chromeless header bar (ZETA-134 review r2 invariant kept). Error tint stays on the outer container's border — the pre-ZETA-135 `tool_state_paints_by_color_alone_and_expanded_body_borders_by_error` test's left-border-color assertion still holds because the new outer container's `.left` border still equals `RAIL_WIDTH_THIN` and carries the same `danger` / `border` color per `is_error`; only the pre-fix "no top/right/bottom border on the body" implicit invariant flips, and it flips deliberately — that IS the trait. The D6 bash reshape and every `whitespace_normal` body invariant survive because the body child is untouched; only the container's border geometry moved. Trait 3 (diff card): edit receipts (any name that classifies as `EditDiff`) build a typed `EditDiffText` from the tool call's `old_string` / `new_string` (or the `old_str` / `new_str` shorthand) at construction time, and the render paints two panes with line-numbered gutters. At wide viewports the panes sit side-by-side (`flex_1` split); below `theme::NARROW_DIFF_STACK_WIDTH = 640px` they stack full-width, remove above add, so the shrunk layout does not collapse each pane's content to one glyph — the row-level layout switch reads the thread-local viewport width refreshed at the top of every `render` (the virtual-scroller row closure has only `&App`). Each gutter's line-number child and each content cell route through the paint-text recorder (`main.rs::paint_text_log` + `record_text_child`, `#[must_use]` under `deny(warnings)`) at row_ids composed by `sel::tool_diff_line_number(pane, idx)` / `sel::tool_diff_line_content(pane, idx)`, so tests assert on WHAT reached `.child(...)`; a renderer that stops passing the number/content to `.child()` drops the recorder call and the sample disappears (round-3 finding: a model-rebuild test still passed because it reconstructed `EditDiffText` from the transcript entry, bypassing the render path). Trait 5 (turn footer): the last transcript row hosts a metadata strip that names the provider · model · duration for the active session, sourced from wire session data + status metrics; every field is `Option`, missing tokens drop, and the footer suppresses itself when nothing survives — no protocol extension. Duration is computed through `chrono::DateTime::parse_from_rfc3339` with a legacy naive-UTC fallback so timezone-tagged timestamps subtract to the true elapsed span and leap-year boundaries (Feb 28 -> Mar 1 2028) report the correct 48h, not the pre-fix hand-rolled parser's 24h. The composed `display` string routes through the paint-text recorder under `sel::TURN_FOOTER` so the footer test asserts on the string handed to `.child(...)`, not a rebuilt `TurnFooterText` (round-3 finding, same shape as the diff-pane recorder). Trait 6 (composer chrome): the composer gains a quiet "ask or steer zeta" label chip as its first v_flex child — muted-foreground tier, `label_small` type role, `mb_1` breathing room, `debug_selector("composer-label")` — plus a wiki-shaped placeholder ("Ask a question or give zeta a new direction…") replacing the pre-fix two-word "Message zeta". Both strings live in `main.rs::chrome` alongside the other view-chrome constants and are consumed by variable reference in the render body, so they add no fence liability (the ZETA-109 renderer-literal fence covers `transcript_render.rs` + `tool_receipts.rs` only). The pre-fix `composer_focus_promotes_the_rail_and_lightens_the_fill` invariant (thick left rail on focus, no top/right/bottom border on the composer FRAME) stays intact — the label chip is a text-only child with no border, no bg, no ring, so no new border quads land inside the composer's bounds. Deliberately out of scope: thought-line body content — `TranscriptEntry::Thinking` carries no body (see the state.rs comment "Thinking body text is never retained — the provider protocol mixes raw reasoning with any display-safe summary channel"); the `+ Thought` prefix already ships on the header. Flat single-column document flow already shipped pre-ZETA-135 via `theme::TRANSCRIPT_MAX_WIDTH = 1024px`, no chat bubbles, and mono-only rendering. Tests: `zeta135_expanded_receipt_paints_inset_panel_with_file_path_header` drives a real bash `ToolStart` + `ToolEnd` sequence, expands the receipt, then asserts (a) the typed seam — `ToolRowText::panel_header` is `Some(excerpt)` when expanded, (b) the render — the `tool-panel-header-0` debug selector paints, (c) the container — the outer `tool-output-0` quad's `border_widths.top/right/bottom` all sit at ≥1px so the wiki inset-panel border shape is on all four sides not just the left, and (d) the argument-less contract — a bare `tool_start` (empty arguments) still opens with `panel_header == None` so the render layer paints no chromeless header bar; `zeta135_tool_row_paints_a_kind_glyph_for_each_family` drives one row per family (bash / edit / str_replace / fetch / websearch / grep / unknown), asserts each row's `ToolRowText::kind_glyph` equals the expected chrome constant, and pins the glyph's spatial position LEFT of the tool label; `zeta135_edit_receipt_paints_a_diff_card_when_expanded` drives a real edit `ToolStart` + `ToolEnd`, clicks the receipt to expand, then asserts (a) the panes paint (bounds, tints, side-by-side above `NARROW_DIFF_STACK_WIDTH`), (b) the paint-text recorder captured `"1"` and `"2"` for BOTH panes at `sel::tool_diff_line_number(pane, idx)`, and (c) the narrow-viewport stacked layout fires at 420px width; `zeta135_turn_footer_paints_below_the_last_row` drives a real session snapshot and asserts (a) the footer paints below the transcript column, (b) the paint-text recorder captured the composed `display` at `sel::TURN_FOOTER` and it contains the provider slug, the model, the middle-dot separator, and the correctly-formatted duration, (c) the offset-aware parser suppresses a duration for two same-instant cross-zone timestamps, (d) the leap-year Feb 28 -> Mar 1 2028 span reports 48h not 24h, and (e) the footer suppresses itself when both session data and metrics are absent; `zeta135_composer_paints_a_label_chip_above_the_input_row` asserts the `composer-label` debug selector paints with non-zero height and its bottom edge sits at or above the `composer-footer` top edge (v_flex order pin). Every pre-ZETA-135 test still passes unmodified: the receipt tests read the row_text model (extended, not renamed) and the `border_l` paint invariant still matches the new `border_1` outer quad because a `.border_1()` quad carries `border_widths.left = 1px = RAIL_WIDTH_THIN` and the correct `danger`/`border` color per `is_error`; the header row's `border_b_1` has `border_widths.left = 0` so it does not enter the pre-fix filter's "thin left rail" set. The renderer-literal fence still passes — every new visible string enters through a typed `ToolRowText` / `TurnFooterText` field or `main.rs::chrome`, and every new selector goes through a new `sel::tool_panel_header(i)` / `sel::tool_diff_line_number` / `sel::tool_diff_line_content` helper. ZETA-124 type roles preserved. Screenshots: `docs/screenshots/ZETA-135-{before,after,expanded-receipt,diff-card}.png` — captured against the shipped build with an isolated `ZETA_HOME`. Simplicity pass over the full diff: dropped an intermediate `panel_body_container` local in `tool_receipts.rs` in favor of chaining `.child(div()...)` directly since it had one caller; kept the `panel_border` local because it is read twice; folded the round-3 raw-name `is_diff_capable(name)` back into the `ToolKind` classifier as `is_diff_capable(self)` so the diff-card gate has ONE fork. Client-only; no protocol or server changes. | ZETA-124, ZETA-125, ZETA-128, ZETA-134 |
| ZETA-133 | Align transcript edges (arc: GUI polish 2, final layout lane). Closes D1 of the two residual audit findings from the wiki-look restyle (ZETA-135); D3 (bottom-anchor for short transcripts) is REPORTED BLOCKED and deferred — see the D3 note at the bottom of this row. D1 residual: after ZETA-135 landed the wiki-look, collapsed receipts (chevron + kind glyph + label) still started ~100px LEFT of prose because prose rode `theme::prose_max_width` while tool rows rode `TRANSCRIPT_MAX_WIDTH` and each row centered independently, so a per-kind centering left the LEFT edges misaligned even though the content itself matched. Contract (a) is the deliberate wiki-look choice: a fixed narrow chevron/glyph gutter hangs LEFT, and the receipts' TEXT (tool name onward) plus the turn footer text sit at the prose left edge; prose, block elements, and expanded panels share that same reading-measure edge. Implementation: the outer `transcript-column` is now always at `TRANSCRIPT_MAX_WIDTH` (unified for every row kind, centered once in the viewport). Inside, every row renders through `transcript_render::transcript_body_pair(gutter, body, body_max_w)` — a `flex-row` whose leading `w=LEADING_GUTTER_WIDTH(38px), flex_shrink_0` child hosts the chevron + kind glyph on tool rows and stays empty on prose / thinking / error / footer / login rows, followed by a `TRANSCRIPT_BODY`-selectored body div whose `max_w` selects the kind's right cap (`prose_body_max_width(base)` for prose + footer, `wide_body_max_width()` for tool receipts / groups / error blocks / expanded panels). Every row kind's body therefore shares the same LEFT edge (`transcript-column-left + px_4-padding + LEADING_GUTTER_WIDTH`), while the chevron + kind glyph paint at `transcript-column-left + px_4-padding` — LEFT of the shared edge, exactly the hanging-indent silhouette contract (a) prescribes. Click / hover / active / group / tab-index / focus for interactive rows (tool receipts, tool groups) move onto the outer flex-row wrapper so a click on the gutter's chevron AND a click on the body's label both fire the same expand toggle; hover-hint reveal scopes across gutter + body via the same `.group(...)` name. Selectors: two new debug selectors — `transcript-body` (body div, shared left edge) and `transcript-gutter` (leading gutter). Tests: `zeta133_body_left_edge_is_shared_across_all_row_kinds` renders each of user / assistant / thinking / tool / expanded tool / error alone at 1500×1000, records `transcript-body.left()`, and asserts every kind matches the prose baseline within 2px; `zeta133_leading_gutter_hangs_left_of_shared_body_edge` asserts `gutter.right() == body.left()` on both prose and tool rows, `chevron.left() < body.left()` (chevron paints IN the gutter), gutter width equals `LEADING_GUTTER_WIDTH`, and tool + prose share the same body left; `zeta133_turn_footer_shares_the_prose_body_left_edge` seeds a session snapshot and pins the footer's left within 2px of a prose baseline. Updated pre-ZETA-133 tests that pinned the OLD per-kind column centering: `transcript_prose_column_caps_at_reading_measure_and_centers`, `tool_rows_keep_the_wide_transcript_column`, `assistant_code_fence_rides_the_prose_cap`, the wedge assistant hanging-indent test, and the recorder wrap-boundary test now check `transcript-body` bounds (against `prose_body_max_width` / `wide_body_max_width`) instead of `transcript-column`. Argued in the PR body: the old column-centering assertion tested a geometric consequence of the pre-ZETA-133 kind-specific `max_w`; under the unified column, the equivalent invariant — prose still wraps at ~88ch, tool bodies still ride the wide cap — is expressed as body bounds vs body caps, plus the centering assertion remains on `transcript-column`, the unified frame that centers once in the viewport. The ZETA-109 renderer-literal fence's mutation-battery test also moved its anchor from a shape only render_thinking_row's pre-ZETA-133 tail had (`.child(header)\n            .into_any_element()\n    }`) to a `.child(header)` shape stable across the D1 body-pair refactor, so the fence's mutation coverage stays strict. The ZETA-127 native pixel-gutter guard reads the rendered `transcript-body` bounds after each capture for its scan edge; it has no reconstructed frame, padding, gutter, or body-edge formula. Round 4 requests 1100px but asserts the achieved CI width is 1024px. It runs one `prose_edge_probe` mutation at 11px; the mutation shifts a full-width probe 8px past the rendered body edge, so the rendered-body scan must trip. Simplicity pass: removed the shared geometry helper and all smoke-side frame, padding, gutter, and body-edge arithmetic; the scan reads the rendered body bound. Simplicity pass: every new visible string enters through `sel::*` or a typed row_text model so the ZETA-109 renderer-literal fence stays strict. Screenshots: `docs/screenshots/ZETA-133-{before,after}.png` — `before` is a verbatim copy of `ZETA-135-after.png` (that IS the pre-ZETA-133 shipped state at 2560085 that this branch was forked from — the D1 residual chevron-left-of-prose ~100px offset is visible directly), `after` shows the shipped shared-edge geometry at the default 1100×760 window with a mixed transcript (prose + tools + turn footer). D3 note (BLOCKED, deferred): the intended D3 approach — a top pad on the virtual list via `MessageScroller::with_list_style(pt(bottom_anchor_pad))` — was implemented and CI-tested but caused the ZETA-107 view-sync stability test (`variable_height_survivor_positions_stay_stable_after_middle_removal`) to fail plus 20+ single-row painting tests (items pushed off the visible list viewport by the pad). Per the ticket's hard constraint, altering ListState positioning semantics violates ZETA-107; the pt on the list DOES alter those semantics (it enters `state.scroll_top` calculations and interacts with `FollowMode::Tail` such that scrolling to `item_ix=0` reveals virtual empty space above item[0] instead of item[0] at y=0). The pt-based path was reverted; the "top-fills-first spacing" hint the contract offered turns out to REQUIRE either (a) the external `gpui_component::MessageScrollerState` exposing `ListAlignment::Bottom` or `max_offset_for_scrollbar()` so we can conditionally apply padding without touching the scroll math, or (b) a POST-PAINT content-height measurement propagated back through ZetaView state to shape the next frame's outer container padding. Both options are beyond this PR's scope (external crate change, or a two-frame layout dance). D3 to be reopened as ZETA-133-D3 with one of those two paths chosen. Client-only: no RPC, protocol, or server changes. | ZETA-124, ZETA-125, ZETA-135 |
| ZETA-139 | Unified content column and base-sized GUI text (arc: GUI polish 2): transcript rows, the pending rail, and the composer now consume one `theme::content_column` frame plus the shared gutter/body helper, with named inner rails for rendered-bound tests. The component theme adapter forces textarea, alert, markdown heading, inline-code, and code-fence text runs to the appearance picker base; a test-only paint recorder checks rendered sizes at 11px, 13px, and 18px. Inner-bound tests pin the transcript body, pending rail, composer rail, input, footer, slash menu, and drop overlay to the shared painted edge. The sidebar dot gap and prose-cap tests use explicit painted tolerances. Client-only; no RPC or server changes. |
| ZETA-132 | Settings shows its sections on open (arc: GUI polish 2, lane 4 of 6). The 2026-09-16 audit hit two paired misses in the Settings surface. C3 — the modal opens with its scroll viewport cut exactly at the `Behavior` section header: approval mode, theme, font, and font size all read as absent, no scrollbar is drawn on the wrapper, and only the ZETA-128 hairline mask cue hints that the remaining sections exist. C4 — the sections `ScrollHandle` persists across close/reopen, so a second open of Settings lands wherever the previous scroll left off instead of the top of the panel. Fix stack, in the order the contract prescribes: (a) resize the modal so the three-section body renders in full at common window heights. `SETTINGS_PANEL_MAX_HEIGHT` moves 560→680 so the panel's `min(shelf, cap)` bound is tall enough to hold Model + Behavior + Appearance + the trailing scroll-cue spacer at 900px+ viewports without the mask ever biting the last row. `SETTINGS_MODEL_LIST_MAX_HEIGHT` stays at 160 — the ZETA-128 credential-error swap test clicks `model-row-2` in a 2-claude + 1-codex catalog whose full-height 156px already relied on that value; trimming the cap to 132 clipped the codex row out of the simulated click's hit region (CI trip on `settings_modal_swaps_models_and_credential_errors_keep_it_open`), and the shipped C3 fix does not need the extra room — the raised panel cap + lowered top offset carry the shape at every common window height on their own. `SETTINGS_MODAL_TOP_FRACTION` uses 0.15 so smaller shelves (the 760px test viewport, laptop windows) also gain vertical room; the shared `MODAL_TOP_FRACTION` remains at 0.25 for rename / delete dialogs. The flat modal still sits below a scrim and inside the wiki-modal silhouette because the max-height cap still bounds the panel's rendered size on tall viewports. (b) On viewports where overflow still bites (760px test viewport, 18px picker), a Kit `Scrollbar::vertical(&self.settings_sections_scroll).mode(ScrollbarMode::Always).viewport_from_layout()` overlay is layered on the relative wrapper alongside the ZETA-128 mask + cue. Kit's Scrollbar hides its thumb when the scroll area fits inside the container (per its `scroll_area_size <= container_size` guard), so the shipped no-scroll case at common window sizes paints zero extra pixels; when overflow does bite, the 8px thumb in the text-normal 20% mix rides the wrapper's right edge continuously (`Always` mode — Kit's default `Scrolling` fades to zero after 2s idle, defeating the "you can always tell it scrolls" affordance the audit asked for). The mask + cue stay: the mask keeps its row-snapped clip so no half-caption straddles its top edge (the ZETA-128 round-7 invariant), and the 1px cue border still reads as "content continues below" for users who never look at scrollbars. (C4) `WorkerMessage::Settings` (the moment the modal actually opens) resets `settings_sections_scroll.set_offset(point(0., 0.))` immediately before flipping `settings_open = true`, so every reopen lands at the top of the panel — not wherever the last wheel or drag left the wrapper. Tests: `zeta132_behavior_section_header_paints_inside_the_visible_slice_on_open` walks 11/13/18 px on the default 1100×760 test viewport and pins the Behavior heading AND the full approval row above the mask's top edge (the audit's exact miss); `zeta132_scroll_resets_to_top_on_every_open` scrolls the sections handle to a non-zero offset at 18px, dismisses via Escape, re-issues the `Settings` worker message, and asserts `offset().y == 0.` on the reopen; `zeta132_scrollbar_paints_a_thumb_when_sections_overflow` opens Settings at 18px and asserts a painted quad matching `theme::palette::scrollbar_thumb()` sits inside the `settings-sections-scrollbar` overlay bounds. Every pre-ZETA-132 test survives unchanged — `settings_panel_height_is_capped_on_tall_viewports` still holds because the cap moved with the assertion; the ZETA-128 scroll-cue mask, focus-trap, tab-stops-inside-viewport, and paint tests keep passing because the mask / focus / row anatomy work is untouched. Theme token assertions in `theme.rs` update to the new values (only the three lines carrying the changed constants). Simplicity pass removed: the scrollbar overlay is a single element inside the already-present relative wrapper — no new focus handle, no bespoke drag machinery, no per-frame plumbing; token changes reuse the existing `min(shelf, cap)` shape rather than introducing a second bound. Scope: `gui/src/theme.rs` (three tokens + their assertions + updated comments), `gui/src/main.rs` (scroll reset on open + scrollbar overlay element), `gui/src/tests.rs` (three ZETA-132 tests in a dedicated region), `docs/design.md` (this row). Client-only: no RPC, protocol, or server changes; the ZETA-109 renderer-literal fence still passes because the scrollbar routes through Kit's theme tokens, not a literal color. Screenshots: `docs/screenshots/ZETA-132-{before,after,after-18px}.png` — `before` reuses the 2026-09-16 audit capture (Behavior header at the mask edge with no content visible), `after` shows the shipped 13px default with all three sections rendered, `after-18px` shows the 18px picker case with the scrollbar overlay live. | ZETA-124, ZETA-128 |
| ZETA-52 | Main-red hotfix: content-sensitive cost assertion (found 2026-08-28 by the ZETA-48 review): `tests/test_slash.py::test_status_counts_compaction_usage` asserts an exact dollar cost derived from live schema/system-prompt byte counts — ZETA-50 (#74) added todo-schema bytes after ZETA-46 (#71) wrote the test, so the combination is red on main while each PR was green (second silent semantic collision; no CI). Fix the TEST, not the feature: compute the expected cost from the same token inputs the code sees (or pin the fake-usage fixture so schema size cannot leak in) — assert derivation, never a hardcoded dollar figure. Sweep test_slash.py for any other exact-cost literals with the same sensitivity. Process rule recorded: post-merge verification on main = FULL suite, not spot checks. | ZETA-46, ZETA-50 |
| ZETA-39 | Prompt-cache breakpoint tuning (arc: cache efficiency, Henry 2026-08-26; lifts the ZETA-23 deferral): audit current Anthropic `cache_control` placement; move any volatile content out of the cached prefix (system prompt and tools blocks must be byte-stable across turns of one session); place the max-4 breakpoints deliberately (system, tools, conversation prefix boundary at the compaction-stable point); compaction must not rewrite the cached prefix except when it genuinely changes. Measure via the ZETA-23 usage counters: add a regression test asserting consecutive-turn cache reads on a fake-usage session, plus governance tests locking placement. Codex side: verify server-side caching is not defeated by request shape churn. | ZETA-23 |
| ZETA-53 | Custom slash commands (arc: macro DSL, Henry 2026-09-02; motivated by the trace-audit daily-driver profile — canned ops chores dominate real usage): load user-defined prompt-template commands from `~/.zeta/commands/*.md` and `<project>/.zeta/commands/*.md` at session start. Command name = filename stem; body = prompt template sent as the user message on invocation; optional YAML frontmatter `description` (shown in autocomplete and /help). Argument substitution: `$ARGUMENTS` (raw tail) and `$1`..`$9` (whitespace-split). Precedence: built-ins always win (loud notice when a file shadows one); project overrides home. Composer autocomplete lists custom commands with descriptions and source badge; unknown-command handling stays on the existing error path. Malformed files fail open with a dim notice, never crash session start. Fake-provider tests use isolated homes (never live ~/.zeta). Tests: loading both scopes, substitution incl. missing args, precedence/shadowing, autocomplete render, malformed-file resilience. | ZETA-19, ZETA-44 |
| ZETA-54 | Exec macros (arc: macro DSL): frontmatter `kind: exec` makes a command file a deterministic shell macro — the body runs as a shell script directly, NO model turn. Output streams into the transcript as a tool-receipt-style card (bounded tail, full log on disk), exit status on the receipt; nonzero exit renders loud. Same approval policy as the exec tool (approval-off mode means no prompt). `$ARGUMENTS`/`$1..$9` substitution before execution; abort key cancels the running macro (process-group kill, canceled receipt). Motivating case: `/rebuild` = the kill-pull-rebuild-relaunch ritual typed ~8 times in the trace audit. Macro runs never enter provider context except as a compact receipt line (cost hygiene). Tests: execution + receipt, streaming bound, failure exit, cancel path, approval gating, substitution. | ZETA-53 |
| ZETA-55 | Command ergonomics (arc: macro DSL): (1) inline-shell substitution in prompt templates — `!`cmd`` spans run before send with bounded output inlined (timeout + size cap, failure inserts a loud placeholder, never blocks the composer indefinitely); (2) `@path` file mentions inside templates resolve through the ZETA-32 attachment path; (3) `background: true` frontmatter on exec macros runs them via the background-task path with completion notification at the next turn boundary (ZETA-48 semantics). Tests: substitution bounds and failure shape, template attachments, background macro notification and cancel-on-exit. | ZETA-53, ZETA-54, ZETA-32, ZETA-48 |
| ZETA-56 | MCP observability + `/mcp` command (arc: MCP QoL, Henry 2026-09-02 "more fluid and easy to use"; today a failed server is invisible outside a log file and mount stalls are silent): `/mcp` lists every CONFIGURED server with live state (mounted / failed+reason / skipped-missing-env / timed-out), transport, tool count, and stderr log path; `/mcp reconnect <server>` tears down and remounts one server (unregister+re-register its prefixed tools, safe while tools from other servers run); mount emits transcript progress notices (dim, one per server) instead of a silent up-to-10s stall; /status gains a one-line MCP summary (n mounted / m failed). Tests: state classification for all four states, reconnect re-registers tools, notice rendering, /status line; fake MCP servers only, isolated home. | ZETA-18, ZETA-46 |
| ZETA-57 | MCP config ergonomics (arc: MCP QoL): per-project `.zeta/mcp.json` overlay merged over `~/.zeta/mcp.json` (project wins per server name, same resolution rules as ZETA-53 command scopes); `/mcp add <name> --stdio <cmd...>|--http <url>` and `/mcp remove <name>` edit the appropriate config file atomically (tmp+rename) and mount/unmount live in the running session — no restart; missing-env and malformed-entry servers appear in `/mcp` with actionable reasons instead of a log-only warning. Auth stays bearer-only (OAuth deferred). Tests: overlay precedence, add/remove round-trip + live mount/unmount, atomic write, malformed-entry surfacing. | ZETA-56 |
| ZETA-58 | MCP resilience (arc: MCP QoL; today a mid-session server crash leaves its tools permanently broken with no recovery): transport failure marks the server DEGRADED — subsequent calls to its tools return a loud error tool_result naming the server and the `/mcp reconnect` hint, never a hang; next invocation attempts one automatic remount with bounded backoff (no retry storms); HTTP 401 surfaces an auth-specific message distinct from connectivity failure. `/mcp` shows degraded state. Tests: crash-then-auto-remount, degraded call error shape, backoff bound under repeated failure, 401 vs connect-refused distinction. | ZETA-56 |
| ZETA-59 | MCP prompts as slash commands (arc: MCP QoL): servers advertising `prompts/list` surface each prompt as `/<server>:<prompt>` in the composer (autocomplete with description + server badge via the ZETA-53 surfaces); invocation maps `$ARGUMENTS`/positional args onto the prompt's declared arguments and sends the resolved prompt as the user message; prompt-list failures degrade to tools-only with a dim notice. MCP resources stay deferred. Tests: prompt discovery, arg mapping incl. missing required arg (loud error), autocomplete render, degrade path. | ZETA-53, ZETA-56 |
| ZETA-60 | Todo widget auto-dismiss (arc: dogfood round 2, Henry 2026-09-04 QoL): once every item in the pinned todo list reaches completed, the widget collapses to a one-line "todos done (n)" receipt and disappears at the next turn boundary; a new todo write re-pins it. Canceled/mixed terminal states count as done for dismissal only when NO item remains pending/in-progress. Tests: all-complete collapse + dismissal, re-pin on new write, mixed-state stays pinned; forced-terminal. | ZETA-33 |
| ZETA-61 | Websearch backend robustness (Henry-hit live 2026-09-04, orch-reproduced: DuckDuckGo's HTML endpoint returns a ~14KB challenge/homepage page with ZERO results for the tool's `zeta/websearch` user agent — the scraper then raises the generic "could not parse results"): (1) make the request shape realistic (standard browser user agent, Accept/Accept-Language headers, POST to the /html/ endpoint as the site itself does); (2) DETECT the challenge/anomaly page and return a truthful typed error ("search provider blocked/served no results page") distinct from parser breakage; (3) fall back to the `lite.duckduckgo.com/lite` endpoint on challenge or parse failure before erroring; (4) fixture tests for: normal results page, empty-results page, the captured challenge page, lite-endpoint parsing. Worker verifies against the LIVE endpoint once and records the evidence in the PR (a live probe is not a repeatable test — keep it out of the suite). | ZETA-13 |
| ZETA-62 | Sub-agent shared budget is session-cumulative (Henry-hit live 2026-09-04, root cause confirmed: `agent_runner.py:253` lazily sets `loop._shared_agent_budget` ONCE and never resets it, so every later top-level `agent` call inherits the depleted budget — after enough sub-agent use, all agent calls fail "exhausted" for the rest of the session; the ZETA-49 contract says the budget is shared per TREE, parent-configured, default = child cap): create a fresh `SharedTurnBudget` per TOP-LEVEL agent invocation; inherit only within that tree (children, grandchildren, background adoptees keep their tree's budget); the exhaustion message names the tree scope, not the session. Governance tests: two sequential top-level invocations each get a full budget; within-tree sharing still enforced (siblings + generations); background adoptee keeps its origin tree budget after root adoption. | ZETA-49 |
| ZETA-63 | Sub-agent introspection tool (arc: dogfood round 2, Henry 2026-09-04: "orchestrator agents must get FULL data about subagents" — observed live: the parent model could not tell which parallel child finished first, background handles are not queryable, task_output is shell-only): every `agent` invocation (foreground and background) returns a stable child HANDLE in its tool_result; new built-in `agent_status` tool takes a handle (or no args = list all children this session) and returns structured data — state (running/completed/canceled/failed), started/finished timestamps, elapsed, turns used vs tree budget, current step (latest child status line), nesting depth, agent_type, and for finished children the final result text. Works on live AND finished children (reads the persisted child JSONL). Parallel-safe (read-only snapshots via the ZETA-41/58 ownership seams). Tests: handle round-trip foreground + background, list-all, live vs finished queries, timing fields ordering (which-finished-first answerable), unknown-handle loud error. | ZETA-37, ZETA-48 |
| ZETA-64 | Sub-agent transcript access + rich completion receipts (arc: dogfood round 2): (1) `agent_output` tool: bounded, paginated read of a child's transcript by handle (offset/limit like the ZETA-34 fetch pagination; renders as compact text with roles; works on live children mid-run and finished ones); (2) background completion notifications carry structured stats (turns used, elapsed, tool-call count, error/canceled flag) instead of bare completion text; (3) foreground tool_result receipts gain the same one-line stats suffix. Provider-context hygiene: stats lines are compact; transcript reads are explicit paginated pulls, never auto-injected. Tests: pagination bounds, live-tail read, stats accuracy against a scripted child, notification shape, receipt suffix. | ZETA-63 |
| ZETA-66 | Live-history teardown guard is attribution-unsafe (recurring gate flake, every lane since 2026-09-03): the ZETA-44 session teardown guard asserts the LIVE `~/.zeta/history` file is unchanged across a suite run, which false-fails whenever Henry uses zeta on this machine during a gate — hit in at least four worker gates, now handled by a per-kickoff rerun clause. Fix the guard to be attribution-safe: assert that no TEST-SPAWNED process wrote to the live home (e.g. isolated-home resolution assertions + a marker/inode check scoped to the test session), not that the live file froze globally. Delete the rerun-clause debt from kickoff templates once green. Tests: guard passes while a concurrent external writer appends to live history; guard still fails when a test process genuinely writes there. | ZETA-44 |
| ZETA-67 | Deflake timing-budget tests (found by the ZETA-64 round-6 gates, 2026-09-05; all three flakes verified pre-existing on main f69a389): (1) `tests/test_agent.py::test_parallel_nested_lifecycle_events_survive_large_batch` fails ~1/8 even in ISOLATION (worst offender — a real flaky test on main, not just load sensitivity); (2) `tests/test_background.py::test_background_start_poll_and_kill_round_trip` and (3) `::test_background_output_ring_trims_at_utf8_boundary` fail under full-suite load at a 1-second `_wait_for_exit` budget (20x isolation green). Fix the tests, not the code: replace hard-coded 1s/short timing budgets with load-tolerant waits (poll-until-condition with a generous ceiling, e.g. 30s, so the assert stays sharp but slow boxes and loaded suites cannot time out) or deterministic completion signals where the code offers them. No production-code changes unless a test reveals a genuine race — in that case stop and report it as a finding instead of patching around it. Verify: each of the three tests 20x in isolation green, plus 3 consecutive full default-env suites green while a parallel CPU load runs (e.g. a second suite in a detached worktree copy). | ZETA-48, ZETA-49 |
| ZETA-88 | Deflake load-sensitive timing tests, round 2 (found during the fundamentals-parity arc gates, 2026-09-05/06; all pre-existing, each hit repeatedly under full-suite load and green in isolation): (1) `tests/test_auth.py::test_error_body_excerpt_joins_large_folded_header_once` (elapsed-time budget assert); (2) `tests/test_hooks.py::test_cancellation_kills_hook_process_group` (the long-standing "hook-timing flake"); (3) `tests/test_tui.py::test_large_markdown_table_falls_back_within_render_budget` (wall-clock render budget). Fix the tests, not the code, per the ZETA-67 pattern: deterministic signals or generous poll-until ceilings; sharp assertions preserved. Verify: each 20x isolated green + 3 consecutive full default suites green under parallel CPU load. Retire the per-kickoff load-flake allowlist clause once green. | ZETA-67 |
| ZETA-89 | Scrub credentials from tool subprocess environments (PR122-AUDIT [medium], 2026-09-06; pre-existing, amplified by the ZETA-87 API-key path): `bash`/`exec`/`run_background` children inherit the full parent env (`tools/bash.py:125`, `tools/exec.py:225`, `tools/_process.py:106`), so a prompt-injected command can exfiltrate `$ANTHROPIC_API_KEY` and friends. Pass an explicit filtered env at the shared subprocess seam: drop the credential basket (`ANTHROPIC_API_KEY`, `ZETA_ALLOW_API_KEY`, plus the standard names in `tests/test_auth.py:14-38`); everything else passes through; an explicit per-call `env` override (if a tool exposes one) still wins. Tests: child env lacks the basket, unrelated vars survive, PATH-dependent commands still work. | ZETA-14 |
| ZETA-65 | Plan mode redesign to Claude/Codex semantics (Henry 2026-09-04: "Not have a yes/no query go from plan -> implementation all in the same turn" — the #90 implementation has the model call an approval-gated `exit_plan_mode` tool whose approval means "leave plan mode and carry it out NOW"): (1) `/plan {prompt}` enters plan mode and, when prompt text is present, submits it in the same action; bare `/plan` and shift+tab toggle entry as today. (2) In plan mode the registry is read-only (keep sharing the PLAN_PRESET definition) and the model is instructed to DELIVER the plan as its assistant answer — the turn then ENDS; DELETE the `exit_plan_mode` tool and its approval flow entirely. (3) Plan mode PERSISTS across turns: follow-up questions and plan revisions stay read-only. (4) Exit ONLY by explicit user action in a later turn: `/plan off`, shift+tab, or new sugar `/implement` (exits plan mode and submits "implement the plan you proposed above"). (5) Sub-agents spawned during plan mode are restricted to read-only presets (explore/plan; `general` is refused with a loud error). (6) Status line + /status keep showing the mode. Migrate tests/test_plan_mode.py: preserve each test's semantic intent, rewrite the flows; add persistence-across-turns and no-same-turn-implementation pins. | ZETA-53 |
| ZETA-68 | Unify sandbox policy across file tools (arc: fundamentals parity A, audit round 3 findings 1-2; 7 live "path escaped sandbox" hits, 4 tilde failures): `write`/`edit` enforce a single-root sandbox while `bash` roams free — the model recovers via heredoc-bash, wasting 1-3 turns per hit and bypassing the write path entirely. Fix: one shared sandbox policy across write/edit/read/bash (either widen file tools to bash's scope or introduce user-registerable workspace roots at session start); every file tool expands `~` per-call (`expanduser`) or rejects it with an explicit "use absolute path" error; sandbox-refusal error text names the currently allowed root(s). Tests: tilde expansion per file tool, cross-root write allowed/denied per policy, error-text contract. | ZETA-24 |
| ZETA-69 | Background-agent handle + budget ergonomics (arc: fundamentals parity A, audit findings 3-4; 6/7 task_output calls failed, two sessions ended in unrecoverable double failure): `agent(background=true)` success text carries no ID, so the model guesses `task_output` with the description string; budget exhaustion throws away all work with an unactionable error. Fix: background-agent start returns the real handle in both text and structured_content plus a pointer to `agent_status`/`agent_output` (never task_output); `agent` gains an optional `max_turns` argument bounded by a sane cap; budget-exhaustion errors state turns used/allocated and preserve the child transcript for `agent_output` reads. Tests: handle round-trip from start text, max_turns respected, exhaustion error shape + transcript readable after death. | ZETA-48, ZETA-63 |
| ZETA-70 | Tool ergonomics cleanup (arc: fundamentals parity A, audit findings 5, 8, 9, 11): (1) bash/exec overlap — unify the argument name and route the docs (long-running work points at `run_background`; timeout errors state the elapsed limit); (2) todo accepts multiple `in_progress` items OR the error suggests the corrective payload; (3) tool errors gain a structured `{tool, kind, hint}` payload (bash errors currently serialize as unparseable prose with null structured_content). Tests: error-shape governance across tools, timeout error text, todo multi-in-progress contract. | ZETA-24 |
| ZETA-71 | Transport decode resilience (arc: fundamentals parity A, audit findings 6-7; 4 tool-killing `Error -3 while decompressing` hits, 2 silent websearch failures): fetch/websearch/run_background HTTP paths die on gzip/brotli header mismatches with no retry. Fix: on decompression failure retry once with `Accept-Encoding: identity`; websearch backend failures surface status + first response bytes in the error. Tests: decode-failure retry path with a fake server, websearch diagnostic error shape. | ZETA-34 |
| ZETA-72 | Headless/print mode (arc: fundamentals parity B; the largest Pi/OpenCode gap — pi `--print`/`--mode json`, `opencode run`): `zeta -p "prompt"` runs without the TUI to turn completion, prints the final assistant text to stdout, exits nonzero on error; `--format json` streams JSONL events (turn lifecycle, tool calls, usage) for scripting; respects session flags (`--continue`, `--resume`, `--no-session` once ZETA-79 lands) and approval policy (headless ask-mode denies with a loud message rather than hanging). AgentLoop is already TUI-independent — this is a second I/O driver with clean exit semantics, not a loop change. Tests: one-shot text mode, JSONL event stream shape, error exit codes, approval-denial behavior; fake providers throughout. | ZETA-29, ZETA-43 |
| ZETA-73 | Settings file (arc: fundamentals parity B; pi two-level settings.json is the model): `~/.zeta/settings.toml` merged under `<project>/.zeta/settings.toml` (project wins per key) for durable defaults — provider, model, approval allow/deny lists, token budget, yolo, theme, keybinding overrides. Flags override settings; `--yolo`/`--no-yolo` are tri-state (either wins over settings, omitted inherits); malformed files fail open with a dim notice. Trust boundary: the project layer may only contribute safe keys (provider, model, token_budget, workspace_snapshot_cap); `yolo`, `[approval]`, `theme`, and `[keybindings]` in a project file are ignored with a loud startup warning so a hostile checkout cannot grant itself approvals, remap `ctrl-c` to exfiltrate the composer, or hide the abort key — a future `/trust` mechanism may relax this per-repo. This gives "always allow tool X" a persistent home in the global file. Tests: merge precedence, flag override, malformed-file resilience, approval-list round-trip, hostile-repo trust boundary, `.zeta/` end-to-end path; isolated homes. | ZETA-53, ZETA-57 |
| ZETA-74 | Stream-stall watchdog (arc: fundamentals parity B, Henry pre-commit; motivated 2026-09-05 by five provider freezes in one day): mid-stream inactivity detection (no bytes for a configurable window) aborts the attempt and auto-retries with bounded backoff, preserving already-streamed partial content per ZETA-43 semantics; a visible "provider stalled, retrying (n/m)" state in the TUI; final failure lands on the ZETA-43 error card with one-key retry. Today only pre-stream retry exists (`transport.py`); mid-stream stalls die silently into stream_diagnostics. Tests: fake-provider stall mid-stream, retry bound, partial-content preservation, TUI state rendering; forced-terminal. | ZETA-43 |
| ZETA-75 | MCP OAuth + resources (arc: fundamentals parity B, Henry pre-commit; today bearer-only locks out most real remote servers): browser/device OAuth flow for HTTP MCP servers with durable token storage + refresh under `~/.zeta` (per-server, never in project files); auth state and expiry surfaced in `/mcp`; 401-triggered refresh before the ZETA-58 degraded path engages; MCP resources listed and attachable via the ZETA-32 attachment path (explicit pulls, never auto-injected). Tests: fake OAuth server flow, refresh-on-401, token persistence isolation, resource attachment round-trip. | ZETA-57, ZETA-58, ZETA-32 |
| ZETA-76 | Workspace file checkpointing (arc: fundamentals parity C; OpenCode /undo-/redo is core, pi ships it as a first-party extension): `/checkpoint` additionally snapshots workspace file state (shadow git commit or equivalent, never touching the user's index/branches); `/fork` restores the tree to the target checkpoint's snapshot; `/undo` reverts to the previous snapshot with `/redo` back. Without this, forking silently leaves the workspace on the abandoned branch's edits. Non-git directories degrade gracefully (conversation-only, dim notice). Tests: snapshot/restore round-trip, fork-restores-tree, undo/redo chain, dirty-workspace safety, non-git fallback. | ZETA-31 |
| ZETA-77 | User-level tool discovery (arc: fundamentals parity C; pi extensions dir equivalent, scoped to tools): auto-discovery extends beyond the package to `~/.zeta/tools/` and `<project>/.zeta/tools/`; malformed tool modules fail open with a notice; discovered tools flow through the same registry validation, approval policy, structured-result contract, and ZETA-73 settings allow/deny lists. TRUST BOUNDARY (mirrors ZETA-73): user-scope modules import and register directly (planted in the user's own home). PROJECT-scope modules are code from a cloned repo — NOT imported and NOT registered at session start; the loud "untrusted project tools present; run /tools trust" warning names them, and `/tools trust` imports+registers them for the current session only (durable trust deferred to a future `/trust` mechanism). Precedence: project wins over USER tools only (post-trust) with a loud "shadows user tool" notice; project tools may NEVER replace BUILT-IN names -- an attempt is REJECTED at the discovery seam with a loud notice naming the module and tool name, and the built-in stays intact (the rest of the project module's tools still register). User-scope shadow of a built-in stays allowed and emits a "shadows built-in" notice. A partial-failed project module cannot silently unregister a pre-existing user tool: prior ToolDefinitions are snapshotted and restored on failure. Import isolation: one bad file does not stop the rest. Tests: both scopes, precedence/shadowing (built-in, user, earlier project), built-in rejection + approval-policy preservation, snapshot/restore of prior tools on failed register(), malformed resilience, import-error isolation, project-tool gating (unknown_tool pre-trust, executable post-trust, warning rendering), approval/settings integration; isolated homes. | ZETA-24, ZETA-73 |
| ZETA-78 | System-prompt customization + context walk-up (arc: fundamentals parity C): `--system-prompt` / `--append-system-prompt` flags plus `~/.zeta/SYSTEM.md` / `APPEND_SYSTEM.md` equivalents (flag wins); AGENTS.md discovery walks parent directories from cwd to root, concatenating nearest-last (today flat two-location lookup misses monorepo subdirectory instructions). Prompt-cache hygiene: overrides must land in the byte-stable cached prefix per ZETA-39 placement rules. Resume: without the flags, resume replays the snapshotted prompt untouched; with either flag, the explicit flag WINS over the snapshot — the context rebuilds from the flags, the snapshot is overwritten, and a visible "system prompt overridden; prompt cache will rebuild" alert prints. Tests: override/append precedence, walk-up order, cache-prefix stability governance, resume+flag override wins, resume+flag append composes, resume without flags untouched, second resume sees the overridden snapshot. | ZETA-25, ZETA-39 |
| ZETA-79 | Session lifecycle ergonomics (arc: fundamentals parity C; audit ritual: the same project re-briefed 5x for lack of discoverable resume): `/new` (fresh session in place), `/name <label>` (shown in the picker), `--no-session` ephemeral runs, `zeta session list|delete|export <sid>` CLI (export = portable JSONL); resume discoverability — session picker shows names + first-prompt preview + age, and the CLI prints "resume with: zeta --resume <sid>" at exit. Tests: new/name round-trip, ephemeral leaves no store entry, list/delete/export, picker rendering. | ZETA-27, ZETA-42 |
| ZETA-80 | Fork from any message + tree view (arc: fundamentals parity C; pi /fork /tree /clone): `/fork` targets any prior user message (implicit checkpoint at that boundary), not only explicit checkpoints; minimal `/tree` branch browser (labels, branch points, current-position marker) navigating the store's existing parent-linked tree; composes with ZETA-76 (forking restores that point's file snapshot when one exists). Tests: fork-from-arbitrary-message, tree rendering, navigation, snapshot composition. | ZETA-31, ZETA-42, ZETA-76 |
| ZETA-81 | Keybindings + themes (arc: fundamentals parity C): `~/.zeta/keybindings.toml` remap layer over the hardcoded map (unknown actions loud-fail at load); theme table with dark/light built-ins + user override file replacing the fixed palette in `tui/theme.py` (light-terminal users are currently unreadable); theme selectable via ZETA-73 settings. Tests: remap round-trip, invalid-binding resilience, theme switch rendering under forced-terminal. | ZETA-73 |
| ZETA-82 | Mid-turn steering + composer escape hatches (arc: fundamentals parity C; pi steering queue, Ctrl+G, ! passthrough): queued composer messages deliver at the next tool boundary of the RUNNING turn (steer semantics) instead of waiting for turn end, with a per-message toggle for the old follow-up behavior; external-editor key (opens $EDITOR, returns text to composer); `!cmd` composer passthrough runs a one-off shell command as a transcript receipt without a model turn (exec-macro machinery, ad-hoc). Tests: tool-boundary injection ordering, follow-up toggle, editor round-trip (scripted $EDITOR), passthrough receipt + approval gating. | ZETA-44, ZETA-54 |
| ZETA-83 | Continuous integration (arc: repo health; no CI exists today — the repo has no `.github/` at all): ZETA-51 and ZETA-52 were both main-red hotfixes where two PRs merged green independently and collided only once on main ("second silent semantic collision; no CI"), and the recorded process rule "post-merge verification on main = FULL suite, not spot checks" is a human standing in for a machine. Add a GitHub Actions workflow running the FULL pytest suite on every pull request AND on every push to main — the post-merge run is the half that catches semantic collisions, since a PR check alone would have missed both incidents. Shape: ubuntu-latest, uv via the official installer, `uv run --frozen` so a transitive bump cannot redden main without a lockfile change in the diff, tmux deliberately NOT installed so the three tmux-gated tests keep their existing skipif path (see ZETA-85), 20-minute timeout, concurrency cancel for superseded PR runs only. Verified live: the first Linux run of the suite surfaced a real pre-existing main bug (ZETA-84) that darwin-only testing could not see. | — |
| ZETA-84 | Linux procfs reports unlinked paths with a " (deleted)" marker (found by the ZETA-83 CI on its first run, 2026-09-05; pre-existing on main and invisible because the suite had only ever run on darwin — survived the ZETA-68 sandbox unification untouched): `_sandbox.py` `_path_from_fd` resolves a descriptor with `os.readlink("/proc/self/fd/N")` on Linux, and procfs appends " (deleted)" once the inode has no remaining links, while the darwin F_GETPATH branch reports the bare path — so the two platform branches are NOT equivalent and only darwin was ever exercised. `tests/test_tools.py::test_write_overwrite_symlink_race_stays_in_sandbox` fails on Linux with `'/tmp/.../target (deleted)' != '/tmp/.../sandbox/target'`. NOT a sandbox escape: containment is enforced by `_verify_ancestry` comparing (st_dev, st_ino) fd identities and never consults the path string. It IS a reporting bug — the write and edit tools surface the resolved path as `structuredContent["path"]`, so on Linux the model is handed a path that does not exist and any consumer doing `Path(result["path"])` gets a bogus path. Fix: strip the marker only when `os.fstat(fd).st_nlink == 0`, so a file genuinely named "... (deleted)" keeps its name; an unlink racing between readlink and fstat is safe because an inode cannot regain links and the endswith guard makes a missing marker a no-op. Tests: pure-function coverage of all four strip/keep cases on every platform, plus a Linux-only integration test pinning bare-path parity with darwin across an unlink. | ZETA-68, ZETA-83 |
| ZETA-85 | tmux-gated tests are not CI-capable (found by the ZETA-83 CI, 2026-09-05): installing tmux on the runner makes the three tmux-gated tests run instead of skipping, and `tests/test_test_environment.py::test_teardown_guard_rejects_tmux_pane_without_home` then fails with `tmux ... wait-for zeta-guard-ready` exit 1. Race: the pane's `sh -c` completes immediately, the session ends, the tmux server exits with nothing attached, and the parent's `wait-for` runs against a dead socket — a test-side timing assumption of the ZETA-67 family that holds on a dev box and not on a fast runner. ZETA-83 ships WITHOUT tmux, so these three take their skipif path and the guard behaviour they cover is a known CI gap, not a pass. Fix the tests so they survive a runner (keep the server alive for the assertion window, e.g. `exit-empty off` or a pane that outlives the signal), then install tmux in the workflow and confirm all three run green. Verify: 3 consecutive CI runs with tmux installed and all three tests executing. | ZETA-66, ZETA-83 |
| ZETA-86 | Argument-scoped approval rules (arc: fundamentals parity B; closes the gap ZETA-73 left open — `ApprovalPolicy.decide()` began with `del arguments`, so there was exactly one knob per tool and a user could not allow `bash` for `git status*` while still gating `rm -rf`, nor scope `edit`/`write` to a subdirectory; in practice that forced either approving every call or `--yolo` for the whole session, and search — the highest-frequency read-only operation — rides on `bash`, the highest-risk tool): a rule is `tool` (bare, matches any arguments, unchanged) or `tool(pattern)`, where the pattern is matched with `fnmatch.fnmatchcase` against the tool's declared **subject argument** — one matching mode, no special suffixes, because on a security surface a misread pattern is an unintended auto-approval. Subjects are declared on the `ToolDefinition` (`approval_subject`, set at registration: bash/exec/run_background → `command`, read/write/edit → `path`, fetch → `url`, websearch → `query`) and pushed into the policy by the registry on every registration and policy change, so discovered (ZETA-24) and MCP-mounted tools are covered without a hardcoded map. Precedence is unchanged: deny > ask > allow > default, and a deny match always wins. Failure modes: an argument-scoped rule naming a tool that declares no subject is a configuration error — dropped from every tier with a loud startup notice, never widened to a bare-name match; a scoped rule whose subject argument is missing or not a string at call time resolves to its safe side (an allow rule does not fire, a deny or ask rule does); a rule that does not parse in `settings.toml` is dropped with a loud warning; the ZETA-73 trust boundary is untouched, so project files still cannot grant scoped approvals. Headless (ZETA-72) neutralises scoped ask rules exactly as it neutralises bare ones, and sub-agents inherit scoped rules through `ChildApprovalPolicy` with delegated prompts still surfacing in the parent. Tests: precedence with patterns, bare-name regression, glob per subject kind, fail-closed subject, subject-less drop + notice, settings round-trip and hostile-project boundary, headless hard-deny of a scoped ask rule, sub-agent inheritance. | ZETA-24, ZETA-72, ZETA-73 |
| ZETA-87 | Optional API-key auth for the Anthropic provider (arc: repo health; motivated by ZETA-83/ZETA-72: benchmarking SWE-bench/Terminal-Bench means hundreds of automated rollouts and hundreds of millions of tokens, far past any consumer plan's allowance and not reproducible for a leaderboard submission — subscription OAuth remains the right default for daily driving but it makes zeta impossible to benchmark, and headless mode from ZETA-72 was the last piece missing before auth): `AnthropicBackend` accepts a credential behind one `AnthropicCredential` interface (`access_token`, `refresh_token`, `auth_headers`, `beta_header`) so the request path stays branch-free — `AnthropicCredentialStore` (subscription OAuth, unchanged default) and a new `AnthropicApiKeyCredential` (static `ANTHROPIC_API_KEY`) both implement it. Gate: `providers/factory.py` only selects the API-key credential when `ZETA_ALLOW_API_KEY=1` is set AND `ANTHROPIC_API_KEY` is non-empty; a bare `ANTHROPIC_API_KEY` with no opt-in is silently ignored (critical regression: plenty of developers have it exported for unrelated tools, and flipping auth mode on its presence alone would spend API credits instead of plan quota with no warning); opt-in set without a key fails loudly at backend-construction time naming both routes (set the key, or unset the opt-in and `zeta login`). API-key headers drop `oauth-2025-04-20` (OAuth-specific) and use `x-api-key` instead of `authorization: Bearer`; a 401 on a static key raises a clear "check your API key" error immediately instead of attempting the ZETA-11 refresh-on-401 dance, which is meaningless without a refresh token. `CLAUDE_CODE_BETA` (`claude-code-20250219`) was probed live against the real Messages API under a raw API key and kept in the API-key beta header (see PR for the request/response evidence) rather than guessed. Scoped to Anthropic only — Codex/ChatGPT API-key support is a separate ticket. Tests: opt-in-absent-with-key still uses OAuth, opt-in-with-key sends `x-api-key` with no `authorization` and no OAuth beta flag, opt-in-without-key fails loudly naming both routes, 401-on-API-key fails loudly without a refresh attempt, full existing OAuth suite unchanged. | ZETA-11, ZETA-72 |
| ZETA-90 | CI matrix + lint job (arc: repo health; follows ZETA-83, which shipped a single `pytest` job on `ubuntu-latest`, so every PR showed one check): (1) run the full suite on `macos-latest` as well — ZETA-84 was a Linux-only bug that darwin-only testing could not see, and the mirror-image gap now exists because CI never exercises the darwin branches (the `F_GETPATH` path in `_sandbox.py`, the `pngpaste` clipboard path in the composer); `fail-fast: false` so one platform's failure still reports the other, the tmux-removal step becomes platform-neutral, and `UV_CACHE_DIR` is pinned to one location so the cache step keys the same way on both runners. (2) a `ruff` job: ruff joins the dev dependency group so `uv run --frozen ruff check` is pinned by `uv.lock` — the verification bar has read `uv run ruff check` since ZETA-83 but ruff was never installed, so reviewers ran an unpinned `uvx ruff` whose default rule set drifts by release; the pre-existing violations (109 across 36 files on 2026-09-07) are grandfathered in `pyproject.toml` `[tool.ruff.lint.per-file-ignores]` per file and rule, so the job fails only on new violations and the list only ever shrinks — fix a file to delete its entry, never add to it in a feature PR. Not included: a Python-version matrix (nothing has bitten) and tmux on the runner (ZETA-85). Known risk: the ZETA-88 load-sensitive timing tests may flake on the slower macOS runner; that is ZETA-88's fix, not a reason to drop the platform. Verified live: the first macOS run of the suite surfaced a real pre-existing stall in `zeta login` (ZETA-91) that darwin dev boxes could not see, the same way ZETA-83's first Linux run surfaced ZETA-84. Verify: three checks on the PR (`pytest (ubuntu-latest)`, `pytest (macos-latest)`, `ruff`), all green. | ZETA-83, ZETA-84, ZETA-85, ZETA-88 |
| ZETA-91 | `zeta login` stalls before printing its URL on hosts whose resolver does not answer for 127.0.0.1 (found by the ZETA-90 macOS runner on its first run, 2026-09-07; pre-existing on main since ZETA-10 and invisible because every darwin dev box answers the reverse lookup in milliseconds): `http.server.HTTPServer.server_bind` calls `socket.getfqdn(host)` on the bind address, a reverse-DNS lookup that blocks until the resolver gives up when nothing answers for `1.0.0.127.in-addr.arpa` — measured at 35.0s on `macos-latest` against 0.0s on `ubuntu-latest` with a throwaway probe step. The spawned login was still alive after 30s with stdout and stderr both empty, and `tests/test_login.py::test_login_sigint_closes_callback_server` failed deterministically across three runs. The resolved name is never used — the server only answers the local OAuth redirect — so bind through a `ThreadingHTTPServer` subclass whose `server_bind` skips the lookup and reports `localhost`. Tests: a pinning test binds with `socket.getfqdn` patched to raise; the SIGINT test keeps its sharp assertions under a poll-until ceiling (ZETA-67 pattern) and now reports exit status, stdout, and stderr when the URL never appears, so a crash and a stall stop looking alike. | ZETA-10, ZETA-90 |
| ZETA-92 | `zeta serve` frontend server (arc: gpui GUI, Henry 2026-09-08 "plan & drive the whole gpui arc"; foundation ticket — a native GUI cannot embed the Python core, so it needs a protocol seam, and the seam makes every future frontend cheap): new `serve` CLI subcommand + `src/zeta/server/` running the existing AgentLoop behind a local endpoint — unix socket by default (`--socket PATH`, default under the zeta home run dir; optional `--port N` on localhost). Protocol: newline-delimited JSON-RPC 2.0 with a hello/capabilities handshake carrying an explicit protocol version. Requests in: list_sessions, new_session, resume, send, steer, approve/deny (by request id), abort, status. Notifications out: turn lifecycle, assistant stream deltas + committed messages, tool receipts (start / bounded output chunks / end), approval requests, sub-agent receipts, usage + compaction markers, loud structured error events. This is a SERIALIZATION layer over existing loop seams — no new core behavior; `server` must not import `tui` (import-boundary test) and the TUI stays untouched. One client per socket in v1; a second connect is refused loudly. A malformed frame returns a structured JSON-RPC error, never a crash. Tests: end-to-end over a real socket with the fake backend (send -> stream -> receipts -> final), approval round-trip incl. deny, abort mid-stream, steer mid-turn, resume, second-client refusal, malformed-frame error, plus governance (import boundaries, module caps). | ZETA-72, ZETA-82 |
| ZETA-93 | gpui GUI milestone 1 (arc: gpui GUI): new Rust crate at `gui/` (own Cargo.toml, not part of the Python package) built on gpui (gpui.rs), speaking ONLY the ZETA-92 protocol — it spawns `zeta serve` itself or connects to `--socket`. M1 scope: session picker (list + new), streaming transcript (user/assistant text + one-line tool receipts), composer (Enter sends, multi-line editing), approval modal (approve/deny), abort key, connection-lost banner with reconnect. NO themes, forks, images, or markdown in M1. Architecture rule: the protocol client is a plain Rust module with zero gpui imports, unit-tested against a scripted fake server over a local socket; gpui views stay thin. CI: a cargo job (fmt --check, clippy -D warnings, test) triggered on `gui/` paths only; the Python jobs are untouched. Tests: protocol client round-trips every M1 message type, stream reassembly, approval and abort flows, handshake version-mismatch fails loudly. | ZETA-92 |
| ZETA-94 | gpui GUI milestone 2 (arc: gpui GUI): transcript rendering parity — markdown (headings, lists, code fences with syntax color), collapsible tool cards with bounded output tails, sub-agent receipt cards, a status bar (model, token spend, cache hit rate via the status request), and light/dark following system appearance. | ZETA-93 |
| ZETA-95 | gpui GUI milestone 3 (arc: gpui GUI): session ergonomics — fork/tree view driving the ZETA-80 seams over the protocol (new serve requests where needed, version-gated), settings surface (model swap, approval mode), image attachments as a protocol extension, and packaging so the GUI launches as a normal Mac app. | ZETA-93, ZETA-80 |

| ZETA-97 | Real models in the GUI (arc: GUI polish): GUI-spawned servers explicitly use Claude and the default Claude model; real sessions list the complete Claude and Codex catalog in provider groups with the current model marked. Idle settings changes rebuild the backend across providers, retain the session and pinned budget, retune unpinned budgets, and report missing logins as RPC errors. Effective fake servers, including plain `zeta serve` without settings, retain their sealed test catalog. | ZETA-95, ZETA-96 |
| ZETA-98 | Rewrite the core GUI on GPUI Kit (arc: gpui-kit rewrite, M1): Kit semantic themes and embedded JetBrains Mono; virtual session previews and transcript; rich markdown with syntax highlighting; Kit composer, approval dialog, reconnect, abort, and status metrics. Preserve the protocol client and connection actor, including RPC degradation and real/fake isolation. Add optional first-message previews to session listings. M2 = ZETA-99: tree/fork, settings, attachments, packaging. | ZETA-95, ZETA-97 |
| ZETA-99 | Restore full session ergonomics on GPUI Kit (arc: gpui-kit rewrite, M2): branch list in the sidebar and a per-message `Fork here` affordance driving `switch_branch`/`fork_message`; a Kit settings overlay with a grouped Claude/Codex model picker (current model marked, selection auto-scrolls into view), approval-mode selection, keyboard nav, and inline credential-error rendering that keeps the modal open on cross-provider RPC failure; image attachments through Kit — file picker plus Cmd-V paste intercept, per-image chips, inline validation errors, `SendImages` dispatch, and post-send attachment history on the transcript row. Packaging remains `make gui-app` (unsigned dev bundle). ZETA-96's RPC-degradation contract survives: a failing branch swap flushes stale transcript state via the worker's status refresh. | ZETA-98 |
| ZETA-100 | Surface tool output and model errors (arc: GUI consumer pass): clickable receipts disclose their stored output tail; failed tools expand by default. Provider errors get a full wrapped block with an Open Settings action. Model changes keep a durable previous provider/model/budget until the first successful provider response; entitlement errors restore that choice and explain the revert. Existing protocol 1.1 settings gating and the sealed fake catalog remain intact. | ZETA-99 |
| ZETA-101 | Add in-app login (arc: GUI consumer pass): settings, credential error blocks, and the first-run empty state share browser OAuth login for Claude and ChatGPT. Reuse the CLI PKCE listener and credential stores; show pending, cancel, success, and typed failures. Advertised protocol 1.1 login RPCs exclude legacy and fake servers. | ZETA-100 |
| ZETA-102 | Polish the GUI consumer experience (arc: GUI consumer pass): isolate settings input and session drafts; preserve text paste; show sent-image thumbnails, thinking feedback, readable status, tool approval summaries, and first-session guidance. Add native menus and fork guidance without new RPCs. | ZETA-99, ZETA-100, ZETA-101 |
| ZETA-103 | Add session rename and delete (arc: GUI consumer pass): gated protocol 1.1 RPCs persist display names, clear names to the derived first-message preview, and delete inactive sessions including corrupt directories. Kit sidebar action menus open rename and delete-confirmation overlays with keyboard controls and visible errors. Active-session deletion returns `active_session` and keeps the transcript intact; select another session first. CLI session rename joins existing list/delete/export commands. | ZETA-100, ZETA-102 |
| ZETA-104 | Mouse selection over the transcript (arc: TUI debugging QoL; the full-screen transcript sits on the alternate screen with mouse reporting on, so a drag never reaches the terminal as a selection, a modifier-drag grabs card borders and margins, and provider errors ellipsized at the card edge so the failing payload could not even be read): mouse reporting adds button-event tracking (`?1002h`, motion only while a button is held; still no `?1003` pointer stream), the transcript widget turns left-button down/drag/up into a selection whose ends are pinned to content (unit key + line text offset, the scroll anchor's identity, with a row fallback for blank separators) and re-resolved to rows on every paint, so the highlight rides along while a reply streams and selecting mid-reply works; it paints the covered cells with the palette's search background and on release copies the covered text (trailing spaces trimmed, blank edge lines dropped) to the system clipboard via pbcopy / wl-copy / xclip / xsel and to prompt-toolkit's in-app clipboard, reporting `copied N lines` in the footer. A plain click clears; only an end whose unit leaves the transcript drops it. Painting is made incremental so this feels live: the fragment lines and the location table are cached per unit, validated by the identity of the unit's cached render string, so a streamed token re-parses one unit instead of the whole transcript (measured 35 ms → 0.4 ms per token at 1.1k lines, 740 ms → 27 ms at 23k lines); the whole-string path remains only while a search highlight is active, and the empty-query search path no longer flattens the transcript. Error cards now wrap the reason instead of cutting it at the edge. Tests: selection geometry and extraction, anchor resolution, fragment highlighting, per-unit cache equivalence against the whole-string parse and the previous location algorithm, single-unit reparse on a streamed token, cache eviction, widget drag/click/scroll flow, survival across streaming appends and a growing unit above, blank-row starts, drop on unit removal, mouse-mode escape sequences, footer notice and clipboards through the app, clipboard tool selection per platform and subprocess failure. | ZETA-20, ZETA-29 |
| ZETA-105 | Ship a self-contained Mac app with an icon (arc: GUI consumer pass): bundle a pinned standalone Python runtime, the installed zeta wheel and locked dependencies at build time; launch the bundled interpreter directly without uv or a checkout. Keep the existing connecting indicator and generate a monochrome zeta icon for Finder, Dock, and Cmd-Tab. Verify relocation, native library paths, offline rebuilds, and startup timing with isolated ZETA_HOME. | ZETA-102, ZETA-103 |
| ZETA-106 | Adopt the wiki run-UI theme foundation (arc: wiki-run parity, lane 1): map the wiki `opencode` palette onto the gpui-kit theme so every zeta surface renders warm-dark, terminal-first, and coherent. Canvas `#1e1e17`, panel `#24241b`, element `#2c2c21`, border `#35352a`, warm off-white text `#ece9d8` with muted `#a19e88` / faint `#716f5e`, single violet accent `#b18bf4` carrying primary/accent/ring/link/caret and both sidebar-primary and list-active-border, semantic tints for success `#a9c957` / warning `#d5d878` / danger `#e2685c`, and 6-16% alpha overlays (`hover 0x0f`, `active 0x24`, `selection 0x29`, `overlay 0x73`). All-mono JetBrains Mono stays; base + mono size land on 15px so per-surface work in later lanes has one number to consume. Radii collapse to 2px, tile radius to 0px, shadows and tile shadows off — elevation is tint step only. Focus ring uses the accent; scrollbar thumb becomes a 20 percent text-color mix that hovers to 40 percent. Overlay backdrops swap black-with-alpha for `theme().overlay` so lane 2/3 can tint them coherently. Token-level rendering tests pin palette + shape + typography; the existing user-turn border assertion continues to match because primary now equals accent. Lane 2 (transcript/composer chrome) and lane 3 (sidebar/status/chrome) consume this foundation; this ticket must not restructure any layout. | ZETA-105 |
| ZETA-107 | Restyle transcript and composer to wiki run chrome (arc: wiki-run parity, lane 2): consume the ZETA-106 theme foundation to repaint the transcript column, composer strip, and footer to the wiki agent-run contract. Transcript centres each row in a 1024px column with 14px rhythm that collapses to zero between adjacent tool rows; user turns are flat rectangles on the element fill with a 3px accent left rail; assistant turns go naked — 2px vertical breath, no bg, no border, no rail; tool receipts sit on one flat 20px line whose state is signalled by color alone (running normal, done muted, failed danger) with a semibold verb and dim detail; expanded tool bodies borrow a 1px indent rail on the panel fill that flips to danger on error. Composer sits on the element fill with a 3px accent left rail dimmed to 62% at rest; focus promotes the rail to full accent and lightens the fill one step — no border, no ring, no other chrome. Send button is INVERTED (text-color fill on canvas foreground, mono semibold, 82px min-width). Footer strip carries a mode word in accent alongside faint metrics and right-aligned contextual hints, with a small pulsing dot that breathes 0.25→1 over 1.2s while streaming or thinking. New tests pin the 1024px column cap and centring, the naked assistant row, tool-body rail colour by error state, composer focus signal (rail width + fill lightening), and adjacent-tool zero gap. Theme extends with transcript/composer/rail tokens and two new palette accessors (`accent_rail_dim`, `composer_focus_fill`) tested against the wiki contract. | ZETA-106 |
| ZETA-108 | Restyle sidebar and status chrome to wiki run UI (arc: wiki-run parity, lane 3): consume the ZETA-106/107 theme foundation to repaint the sidebar column, connection banner, footer status strip, and modals to the wiki agent-run contract. Sidebar pins to a 216px fixed column with a 44px header band, a ghost `New session` action, and 40px session rows on 5px x 8px padding. Current-session rows paint NO fill — transparent bg, primary label at semibold in the accent tier, plus a small accent dot in the left gutter — instead of the Kit selected-tint fill; hover on inactive rows fills to the element tint. A 2px danger attention rail pins the gutter of every row while the connection is lost. Branch rows nest at 32px height with 40px indent per depth level and their own accent dot on the current branch. Connection-lost and command-error strips both paint as wiki "blocker rows" — 2px danger left rail on a 10% danger tint, no framed alert card. Reconnecting borrows the same shape in the accent tier. Footer status strip lands on the header band-2 rhythm (40px min-height): a solid state pill carries the single mode word (neutral = accent, offline = danger), separated from the metrics label and the contextual hint by 1x14 vertical rules; the streaming dot rides beside the pill. Modals (Session settings, Rename/Delete) become flat panels — 480px wide on the panel token, no border, no shadow, seated below the scrim at ~25% of viewport height; padded 12/16/14; title band pairs a 15px semibold heading with a plain-text `esc` at the right; rename input paints a bottom-only underline, no box; action buttons drop to 30px min-height. Theme extends with sidebar / attention rail / header band / state pill / status rule / modal shape tokens plus a new palette accessor `danger_tint`, tested against the wiki contract. New visual tests pin sidebar width and row height, current-item no-fill + accent dot, blocker-row rail + tint, state-pill fill neutrality and offline danger flip, modal flat-panel shape, and the two status-strip vertical rules. | ZETA-107 |
| ZETA-110 | Polish inline code, run header, and table chrome (arc: wiki-run parity, post-lane cleanup): the shipped ZETA-106/107/108 chrome still had three visible defects in the running app. (1) Inline `code` chips inside markdown tables clipped their trailing glyph (`bas` / `tod` / `rea` / `edi` instead of `bash` / `todo` / `read` / `edit`) because gpui-base's default wrap-table layout budgets columns by character count and every cell wraps its content in `overflow_hidden()`; on the 0.875-scaled + 4px-padded inline-code chip the shaped advance overruns the char-count budget and the trailing glyph is sliced off by the flex row. (2) The same chip painted on `theme.accent` — a solid violet slab — that visually competed with real state chrome (pills, current-item text). (3) The run header's band 2 duplicated the composer hint (already carried by band 1's step text) and pinned the model name on its own, with the middle metrics slice ALSO printing the model, so the model appeared twice and clipped at the right window edge. Plus a full grid on markdown tables (per-cell vertical borders) and a cramped sidebar tail where the fork hint + Branches strip visually collided with the composer strip. Fixes, all in TextViewStyle overrides for the assistant TextView + one main.rs band rewrite + one sidebar edit + a status_label refactor: inline-code chip now paints subtly on a text-normal 6% tint with normal-tier text (new `palette::inline_code_bg` / `inline_code_fg` accessors + a WCAG-hue-aware test); assistant TextView opts into gpui-base's scroll-table layout and sets `white_space: nowrap` on `table_cell` so column floors rise to the shaped chip width and the chip's trailing glyph always lands inside the column; a transparent per-cell border color kills the vertical grid so the table reads as flat rows; band 2 drops the duplicate composer_hint entirely and refactors `polish::status_label` to be model-free so the middle "usage" slice never repeats the pinned right model chip, which now `max_w(220)` truncates before crowding the window edge; sidebar's fork hint gains a top border + margin at the sidebar-border tier so the tail separates from the composer chrome, and the branches container gains a bottom pad. The band-2 rule-count test now pins exactly one rule between metrics and model (a regression that reintroduces the composer-hint duplicate would push two rules and trip the assertion). No RPC, server, or session-logic changes. | ZETA-108 |
| ZETA-111 | Add appearance settings — font size, font family, theme (arc: UX polish, lane 1 of 4): make the shipped `opencode` look adjustable without leaving it. The default drops to 13px (from 15px) and moves into `Appearance::default()` — a new `theme::Appearance { theme, font_family, font_size }` struct that `apply_with(cx, &appearance)` consumes to reflow every gpui-kit surface live (colors, typography, radii, syntax highlight, base scrollbar). Five palettes ship — `Opencode` (default, dark warm-off-white on `#1e1e17`), `GruvboxDark`, `VscodeDarkPlus`, `Nord`, `GruvboxLight` — each a `Palette` struct with base tokens + a `SyntaxHex` map; alpha-derived tokens (`hover`, `active`, `selection`, `overlay_strong`, `scrollbar_thumb`, `scrollbar_thumb_hover`, `accent_rail_dim`, `danger_tint`) derive from base via `with_alpha(base, ratio)` so a palette author fills base only. The `palette::*` accessors keep their names; each reads through a process-wide `ACTIVE: LazyLock<RwLock<Appearance>>` so render-time callers pick up the new palette without threading `cx`. Five monospace families are offered (`JetBrains Mono` default + `Fira Code`, `SF Mono`, `Menlo`, `Monaco`); gpui's text system falls back gracefully for anything the platform cannot resolve. Font size is a whole-px stepper across the 11-18px picker window; the 22px baseline rhythm rides on `rems(1.65)` so it already scales with the base size. Settings > Appearance is one modal section with three horizontal button rows (theme / font / size ±) below Model + Approval mode; clicking a control calls `prefs::commit(cx, appearance)` which live-applies AND writes `$ZETA_HOME/gui-prefs.json` atomically (tmp + rename). A missing or corrupt prefs file resolves to `Appearance::default()`; unknown theme slugs / uncurated font families / out-of-range sizes each fall back to their default. `main()` loads prefs before opening the window so the first frame paints on the user's palette; `ZetaView::new` never touches disk (tests get a deterministic baseline via `theme::apply` in `setup`). Mutators anchor on the PER-APP `cx.theme()` (via `app_appearance`), not `theme::current_appearance()`, so parallel gpui-test workers cannot race the shared `ACTIVE` slot between read and write. New tests: prefs round-trip + corrupt/missing/uncurated fallback; `apply_with(GruvboxLight)` shifts canvas/foreground/accent off opencode; every shipped theme clears WCAG-AA text-on-canvas contrast; a full click-flow test drives the theme / font / size controls end-to-end. Screenshots: `docs/screenshots/ZETA-111-{before,after}.png` show the added Appearance section; four `ZETA-111-theme-<slug>.png` shots show each non-default palette. Client-only: no RPC, protocol, or server changes. | ZETA-106 |
| ZETA-109 | Type the transcript row-text seam and fence renderer literals (arc: wiki-run parity, ZETA-107 follow-up): replace the positional `TranscriptEntry::visible_text -> Vec<&str>` seam with a typed row-text model — one struct per row kind (`UserRowText`, `AssistantRowText`, `ThinkingRowText`, `ToolRowText`, `ErrorRowText`) plus `LoginRowText` for the login surface, whose fields name every user-visible string the row paints: content, action/button labels (`Fork here`, `Open Settings`, `Log in with …`, `Cancel`), attachment names + sizes, tool output sizes, hover hints, provider status, and the `Earlier output omitted` marker. All six transcript renderers plus the login row live in ONE module (`gui/src/transcript_render.rs`); the AST fence scans the whole module rather than a fixed function-name list, so renaming a renderer or moving text into a fresh helper fn cannot smuggle a literal past the guard. Renderers destructure each model and consume every field so a dropped painter fails clippy's `unused_variables` under the crate's `-D warnings` gate. Fixed chrome constants live in one `row_text::chrome` module the model and the fence's `ALL` sweep both reference; widget-ID composition (`format!("tool-verb-{i}")`, `Button::new(("fork", i))`, `format!("{prefix}-{provider}")`) lives in `row_text::sel` so the render module contains no bare literals outside allowed selector-method args. A static `renderer_literal_fence` guard test parses the render module with `syn` (dev-dep) and rejects every string / byte-string literal in expression position that is not an argument to `.debug_selector(...)`, `.id(...)`, `.aria_label(...)`, `.role(...)` — or sitting inside a diagnostic macro payload (`panic!`, `unreachable!`, `todo!`, `unimplemented!`, `assert{,_eq,_ne}!`, `debug_assert{,_eq,_ne}!`). AST context — NOT string shape — separates an ID from visible text, closing the r1 review's `.child("done")` / `format!("tool-verb-{i}")` / `b"…"` / helper-fn / renamed-fn bypasses at once. Mutations against the actual module trip the fence: bracketed state markers, lowercase prose, prose format fragments, ID-shaped format fragments, byte strings, and text moved into a new helper are all caught; a bare rename is correctly accepted because it introduces no new literal. A chrome-coverage arm parses `row_text.rs` and proves every `pub const` in `chrome` sits at exactly `pub` visibility (no `pub(super)`/`pub(crate)`/private) and appears in `chrome::ALL`. The user-row `attachments` field is a tri-state `Option<Vec<String>>` so text-only history rows (attachment key present but empty) keep the same `mt_1` container and identical row height as the pre-typed-seam renderer painted. Borrowing preserves the ZETA-107 r5 invariant — content-shaped fields hold `&str` into the entry; only composed labels (attachment lines, output-size string, login labels) allocate. Contract line 83 privacy invariant preserved — the `Thinking` row's model carries exactly `THINKING_HEADER_LABEL`. No visible rendering change. | ZETA-107 |
| ZETA-117 | Arrow-key model picker (arc: TUI QoL; `/model` demanded the exact provider id, so `/model opus` went to Anthropic verbatim and 404ed, and the only way to learn a valid id was to read the source): `/model` with no argument draws a picker card listing the provider's known models — the static `PROVIDER_MODELS` table merged with the live catalog once it loads, refreshed in place if the catalog arrives while the card is open — with the current model marked and context windows shown; ↑/↓ move (wrapping, windowed for long catalogs), enter runs `/model <name>` through the normal pipeline, esc cancels, and the keys stand down whenever the composer has text, mirroring the approval y/n shortcuts. `/model <text>` switches directly on an exact or unique substring match, narrows the picker to multiple matches, and keeps the old "use as typed" path for text matching nothing so brand-new ids still work. The composer completes model names after `/model `. Same behaviour for Claude, Codex, and the fake provider. The slash-command menu itself is restyled from prompt-toolkit's gray-on-gray default to the palette (rows on the search background, the current row on the accent with a palette-chosen text colour) and re-homed from the composer's own float container, where it could only draw inside the composer box, to a float container spanning the screen, so it opens above the composer with up to 12 rows and a mouse click picks an entry. Tests: catalog union and matching, picker navigation/window/render, direct vs narrowed vs unknown switch, full-screen card removal on cancel, catalog refresh while open, keys only on an empty composer, completer, menu styles, menu float placement and render above the composer, click-to-pick. | ZETA-19, ZETA-97 |
| ZETA-112 | First-class image attachment UX (arc: UX polish, lane 2 of 4): promote the composer's plain "Attach image" text button to a Cursor/Conductor-style attachment surface fed by three input routes and a proper chip row, image-only per Henry's scope decision, GUI-only. The button becomes a 40x40 icon-only affordance (a `+` glyph, `IconName::Plus`) with a tooltip that spells the full surface — "Attach an image · drag files in · Cmd-V pastes" — and shares the composer action-row height with Send (`SEND_BUTTON_HEIGHT`) so the two controls read as one row, a shape a new `attach_button_paints_an_icon_hit_target_at_send_button_height` test pins by asserting attach ≥ 40x40 AND `attach.size.height == send.size.height`. Drag-and-drop: the composer div gains an `on_drop::<ExternalPaths>` + `drag_over::<ExternalPaths>` pair, and a full-composer drop-target overlay (`composer-drop-target`) paints absolutely inset over the pending strip — dashed 1px `drag_border`, `drop_target` fill, title "Drop image to attach" + hint "PNG · JPEG · GIF · WebP · up to 512 KiB" — so the drop zone reads as one flat surface rather than a per-cell border flicker. Overlay is scoped to the composer's hitbox (not just window-wide `cx.has_active_drag()`) via an `Rc<Cell<bool>>` the `drag_over<ExternalPaths>` closure sets each paint it fires; `render_composer` reads the previous frame's value then resets it, so a drag that leaves the composer bounds — even without leaving the window — clears the overlay within one repaint (`drop_overlay_hides_when_the_drag_leaves_the_composer_bounds`). The `drag_over_composer_paints_drop_target_and_drop_adds_attachments` test dispatches `FileDropEvent::Entered/Exited/Submit` against a real PNG on disk and verifies the overlay lights up, clears on exit without attaching, and on submit routes through `attach_from_paths` to render a pending chip. Clipboard paste: a `paste_image` handler (`capture_action`, gpui `input::Paste`) fires before the textarea's text paste; when the clipboard carries `ClipboardEntry::Image` it decodes into an `ImageAttachment` (`pasted-image.<ext>`), when it carries `ExternalPaths` it routes through `attach_from_paths`, and `String` payloads fall through to the normal text paste — the `attachments_paste_shows_chip_and_send_dispatches_send_images` test primes the clipboard with a PNG, dispatches `cmd-v`, and asserts a chip renders, `SendImages` fires with the right batch, and `WorkerMessage::ImagesSent` clears the chip AND the pending-user-turn strip. Chip row: pending attachments are a typed `PendingAttachment` enum (`Valid { image, thumbnail }` or `Invalid { name, error }`) that renders as a wrapped horizontal row (`composer-chip-row`) of chips. Valid chips pair a thumbnail (token-scaled via `theme::chip_thumbnail_size` — subtle 10% white-on-dark / black-on-light outline matching the transcript's sent-image thumbnails) with a truncated filename and a byte-size line (`polish::format_bytes` — "512 B", "42 KB", "0.3 MB"), plus a token-sized icon-only remove button (`IconName::Close`, per-chip debug selector `chip-remove-{index}`); a preview decode failure demotes the entry to `Invalid` at attach time (`ATTACH_DECODE_ERROR`), so a `Valid` chip always paints a real thumbnail and the render path never has to reason about a missing preview. Invalid chips share the frame (`composer-chip-error-{index}`) but paint a `TriangleAlert` glyph against `danger_tint`, name + inline error message in place of size, are NEVER included in Send, and carry `Role::Alert` + an `aria_label` pairing filename with the full error text (plus a matching tooltip) so screen readers announce them and sighted users see the full error even when the visible label truncates. Chip surface uses `theme().muted`/`theme().border` so every one of ZETA-111's five palettes gets a legible chip, and every dimension (thumbnail, remove-button, padding, label max-width) routes through `theme::chip_*` tokens so the whole row scales with the appearance picker — pinned by `attachment_chip_scales_with_the_appearance_font_size` at the 11px floor vs. 18px ceiling and `attachment_chips_render_legibly_across_every_shipped_theme` iterating `ThemeId::ALL`. Thumbnail asset-cache leak fixed: `remove_attached_image` and `clear_composer_images` both call `image.remove_asset(cx)` on every held preview so repeated attach/remove cycles do not leak GPU storage — verified by `removing_and_clearing_pending_chips_evict_thumbnail_assets` asserting `is_asset_cached` flips false after both paths. Multi-attachment + mixed-batch survive: `attachment_chip_renders_remove_button_and_multi_attachments` seeds three valid attachments and drops the middle; `per_file_parse_errors_render_their_own_chip_and_never_ship` seeds a good + bad pair and asserts the banner stays empty, both chips render, and Send fires with only the valid image; `attachment_parse_errors_cover_all_four_kinds` covers unreadable path, oversize file, undecodable bytes, and unsupported format. Batch validation: `add_pending_attachments` still guards `count > MAX_ATTACHMENTS || total valid bytes > MAX_IMAGE_BYTES` and surfaces `chrome::ATTACH_LIMIT_ERROR` inline via the existing ZETA-100 alert strip; the `drop_reuses_the_batch_limit_error_and_leaves_chips_intact` test drives an over-cap drop and asserts the banner fires without dropping any chips. Shared attach path: a new `attach_from_paths(paths) -> add_pending_attachments` seam collapses the file-picker, drag-and-drop, and clipboard-paths routes onto one batch flow so every entry surfaces the same per-file `Result<ImageAttachment, (name, error)>` chip pair; clipboard-single (paste image) keeps the batch banner on decode failure so a macOS mixed-clipboard paste still falls through to the text-paste path. Attach button click actually invokes the picker — `attach_button_click_invokes_the_file_picker` asserts `did_prompt_for_paths()` after a real click. Composer chrome literals sit in a `chrome` module at the top of `main.rs` — attach label, attach hint, drop-target title/hint, chip remove label, attach-limit error — outside the ZETA-109 renderer-literal fence (composer chrome is view chrome, not a transcript row) so the fence's `.debug_selector`/`.id`/`.aria_label`/`.role` rule stays untouched. New helper `polish::format_bytes` returns the compact byte label used on chips. `MAX_ATTACHMENTS: usize = 4` centralises the count cap (was an inline `4`). `paste_image` continues to propagate when the settings modal or session-edit is focused so those surfaces keep the platform paste. All new tests pass, `cargo fmt --check` and `cargo clippy --all-targets -- -D warnings` are clean; screenshots `docs/screenshots/ZETA-112-{before,after}.png` show the plain-text button vs. the icon + chip row. No RPC, protocol, or server changes — send still routes through `send_images`; sent-image transcript thumbnails and the ZETA-107/110 ordered-edit view-sync stay intact. | ZETA-111 |

| ZETA-119 | Composer ergonomics (arc: harness parity, lane 3 of 5): complete filesystem paths inside `@` tokens in the TUI with bounded one-level listings, quoted names with spaces, hidden-entry gating, and continued directory drill-down. Add deterministic static `zeta completion zsh|bash` scripts covering top-level flags, subcommands, and nested session and automation verbs. | — |
| ZETA-111 | Unattended job approval boundary: a fresh deny-by-default policy uses only the job's ZETA-86 rules; registry enforcement covers approval-exempt tools, internal skip paths, and child clones. Extract configured session construction and turn driving into `runtime/`, preserving TUI/headless interfaces and test seams. No automation yolo mode. Tests: listed/scoped allow, unlisted/exempt/internal denial, child inheritance, global-yolo isolation, existing headless/session regressions and import/module governance. | ZETA-72, ZETA-86 |
| ZETA-112 | Automation storage and pure scheduling under `automations/`: SQLite behind a Protocol, immutable revisions, transactional occurrence claims, tagged schedule/poll triggers, UTC cursors and source-event dedup. Five-field numeric cron; America/Toronto default; latest missed occurrence catches up for at most two hours; nonexistent local times skip and repeated local times run once. Arming starts eligibility at approval. Bad entries stay inert with visible errors. Tests: cron boundary/range/list/step/day matching, DST, read-only fake-clock tick, claim races, last-run dedup, catch-up limits, malformed imports/stored entries. | ZETA-111 |
| ZETA-113 | Restricted automation runner: every claimed attempt has a normal resumable session. Mount selected home MCP servers plus the fixed Slack delivery service; omit project overlays, external Python tools and lifecycle hooks. Poll every five minutes with a bounded last-run window and validated source IDs/timestamps; consume positive events before execution, advance negative checks, retain the cursor on failed checks. Default 25 iterations per phase and ten-minute total timeout. Tests: positive/negative/invalid polls, old/future/repeated events, consumed-event failure semantics, denied-tool receipts, approval recheck and resume. | ZETA-112 |
| ZETA-114 | Model drafts, human grants: discovered `automation` tool only lists/inspects/saves inert drafts; `/automations` and `zeta automation` expose inspection, import, exact-revision approval, and disable. Review displays resolved permissions, schedule, provider/model/cwd and recipient, then requires the matching review token; terminal approval requires interactive input. Imported project grants are inert snapshots; edits suspend future runs pending reapproval. Tests: tool cannot arm, draft/approve/persist round-trip, stale review rejection, project-file isolation, explicit defaults and CLI/slash discoverability. | ZETA-112 |
| ZETA-115 | Official Slack MCP delivery and preregistered-client OAuth: home MCP auth supports client ID/secret, fixed callback port and scopes through existing env interpolation, PKCE, secure token storage and refresh; dynamic registration remains unchanged for other servers. Home `approval_subjects` maps remote tool names to declared arguments without inventing scopes. Resolve and human-approve a fixed recipient; harness sends one bounded final message independently of model grants and records the response in the normal session. Tests: fixed-client code/refresh flow without registration, auth config round-trip, scoped MCP execution, ambiguous-recipient refusal, bounded destination-pinned sending, and uncertain-send no-replay. Live verification requires working Linear/Slack credentials and a designated test recipient; the initial Linear mount returned 401. | ZETA-18, ZETA-86, ZETA-113, ZETA-114 |
| ZETA-116 | Foreground `zeta automation daemon` for a single-tenant VPS: one worker, 30-second scheduling loop, singleton lock, responsive SIGINT/SIGTERM shutdown, and per-job failure isolation. Interrupted attempts and uncertain sends are durable and never automatically replayed; future triggers continue. No webhook/control API or separate service. Tests: singleton exclusion, responsive shutdown, restart recovery, due-job isolation, and full draft → approve → fire → deliver → inspect → resume workflow; full pytest and Ruff gates on the existing Linux/macOS CI matrix. | ZETA-113, ZETA-114, ZETA-115 |
| ZETA-117 | Full skills system (arc: harness parity, lane 1 of 5): discover packaged, home, and project skills once per session with project-over-home-over-packaged precedence; support flat files and Claude-Code skill directories with bundled resources; accept Claude-compatible frontmatter with optional keywords and skip malformed files with visible warnings; expose each skill through the `skill` tool and slash commands after built-ins and custom commands, with completion source badges and collision warnings; keep the escaped system index deterministic and bounded to 32 KiB. Project skills are prompt content and need no trust gate; referenced scripts use normal bash/exec approval. | — |
| ZETA-118 | Custom agent definitions (arc: harness parity, lane 2 of 5): discover Claude-compatible home and project agent markdown with project-over-home-over-packaged precedence, safe names, model validation, warnings, and fixed-root symlink containment. Snapshot the selected definitions per session; expose the same names in the agent schema and enforce custom tool allowlists and prompt/model overrides in foreground children, while automation sessions mount packaged presets only. | ZETA-117 |
| ZETA-123 | Restructure GUI chrome and sidebar (arc: GUI polish, lane 1 of 4): fix chrome that reads as cluttered on the running app — two stacked header bands, a permanently-visible per-session `...` menu, a docked fork hint, a centered "New session" heading, and a model chip pinned inside the composer input row. Collapse the header to ONE 44px row: session title on the left; a right-aligned metadata cluster with quiet tokens/cache, a small dot + state word (glyph, not a filled pill), and the model name — rules separate the three metadata slots. Sidebar restructures to three zones: title + "New session" as an ACTION (ghost button, `+` glyph, `.compact()` sizing, not a modal-CTA heading); session list with the `...` menu revealed by per-row `.group()` hover (each row scopes its own reveal so one pointer never lights up every menu); Branches only when they exist. The docked "Hover over your message to fork from it" hint is deleted — the transcript fork button already reveals on the user row's own hover. Composer chrome: model target moves OUT of the input row into a compact footer row that also carries the keyboard hint (`Enter sends · Shift-Enter adds a line`); Send switches to the primary/accent variant, sized to its label, paired at 40x40 with the icon-only attach so both controls read as one action row. Behavior unchanged — every existing action (send, attach, fork, rename, delete, settings, branch switch) keeps its test coverage; layout tests pin single-row header, no `run-header-band1` / `status-bar` selectors, hint present in composer footer and absent from header, sidebar `new-session-button` as an action, session-menu opaque-paint-free at rest. Theme tokens `HEADER_BAND2_MIN_HEIGHT` and `STATE_PILL_PADDING_{X,Y}` retire with the old pill; `HEADER_BAND1_MIN_HEIGHT` stays the single header floor. laws-of-ux applied: Proximity (metadata next to what it describes: model + hint in the composer footer), Prägnanz (one header row reads simpler than two), Chunking (three sidebar zones the eye groups without effort), Selective Attention (the quiet dot + word survives the header's noise budget). Screenshots: `docs/screenshots/ZETA-123-{before,after}.png`. No RPC, protocol, or server changes; no touches to `transcript_render.rs` or the ZETA-109 renderer-literal fence. | ZETA-112 |
| ZETA-136 | Deflake approval-dialog dispatch assertions (arc: GUI polish 2, lane 8): the click fixture dispatches the real mouse-down and mouse-up events inside one GPUI update, so no task drain can occur between them. The mouse-up reaches the `approval-always` listener, `decide_always_tool` latches `approval_pending`, and `queue` sends exactly one `ApproveAlwaysTool` message. The keyboard fixture keeps the same synchronous command assertion because it crosses the same handler and channel boundary. Duplicate-input and idle-status-close assertions remain. No production behavior changes. | ZETA-131 |
| ZETA-133-D3 | Bottom-anchor short transcripts via list alignment (arc: GUI polish 2, ZETA-133 residual). Chat-UI trait: a transcript SHORTER than the viewport rests on the viewport's bottom edge; once content exceeds the viewport, alignment collapses into normal scrolling and tail-follow still pins the newest row. The ZETA-133 D3 first attempt padded the top of the virtual list from window height and broke the ZETA-107 view-sync stability test (`variable_height_survivor_positions_stay_stable_after_middle_removal`) plus 20+ painting tests because window-height-derived padding poisoned the list's scroll math. This ticket takes the alternate path Henry chose from that PR's residual note: gpui's virtual `ListState` already supports the mode via `ListAlignment::Bottom`, so the fix lives inside the list's own supported behaviour rather than around it — no changes to scroll-offset math, item measurement, or view-sync plumbing in zeta code. `gpui_component::MessageScrollerState::new` in 0.6.1 hard-codes `ListAlignment::Top` and exposes no seam; the crate is vendored into `gui/vendor/gpui-component/` (byte-identical to crates.io except the ONE authorised edit) following the ZETA-129 fork precedent, and `MessageScrollerState::new_with_alignment(item_count, alignment, cx)` becomes the smallest possible seam — `new` delegates to it at `ListAlignment::Top` so every upstream caller keeps shipped behaviour. `gui/Cargo.toml` gains `[patch.crates-io.gpui-component]` so `gpui-kit`'s transitive use picks the fix up too, and the transcript's `MessageScrollerState` is constructed with `ListAlignment::Bottom` at `main.rs:374`. Tail-follow is still enabled inside `new_with_alignment`, so live streaming continues to pin the tail; bottom alignment plus scroll_to_end do not double-anchor (the two mechanisms operate on the same scroll edge). Tests: `zeta133_d3_short_transcript_rests_on_viewport_bottom_at_default_font` seeds one user row on an 1100×1200 viewport and asserts the rendered `transcript-body` bottom sits within ~48px of the `transcript-viewport` bottom (i.e. the list's own `py_2()` pad, not the ~1170px a top-anchored regression would produce); `zeta133_d3_short_transcript_rests_on_viewport_bottom_at_max_font` repeats the assertion at the picker's 18px MAX so the row grows taller but the bottom-anchor delta stays a small pad; `zeta133_d3_tall_content_keeps_last_row_pinned_at_viewport_bottom` seeds 40 wide-wrapped assistant rows into a 1100×400 viewport (content > viewport), asserts `is_following_tail() == true` and `is_scrolled_up() == false`, and pins the last painted row's `transcript-body` bottom near the viewport bottom — exactly the pre-D3 tail behaviour, guarding against a bottom-alignment regression that poisons the tall-content scroll math. The ZETA-107 stability fence now deliberately uses the tall-content path: it resizes to 1100×400, scrolls to item 1 so the surviving tool rows remain painted, asserts `is_scrolled_up()` and `!is_following_tail()` before the middle removal, then requires both surviving rows to shift positively and equally, with the existing adjacency-gap check still in place. Screenshots: `docs/screenshots/ZETA-133-D3-{before,after}.png` — `before` is a fresh controlled `ListAlignment::Top` capture of the top-anchored short transcript, and `after` shows the bottom-anchored short transcript on the same window. Simplicity pass over the vendor edit: the seam is one new constructor plus a one-line delegate; every other fork file stays byte-identical to the crates.io release. Client-only; no RPC, protocol, or server changes. | ZETA-133, ZETA-107 |
| ZETA-138 | Fit Settings in view without scrolling (arc: GUI polish 2, follow-up to ZETA-128/132). The Settings panel now packs to its content with `.max_h(min(viewport shelf, SETTINGS_PANEL_MAX_HEIGHT))`; it has no fixed height or flex-grow spacer. One bounded `.overflow_y_scroll()` body contains the Model, Behavior, Appearance, Claude auth, ChatGPT auth, optional error, and Close/Apply footer. At 1100x800 and 1500x1000 the full content fits without a scrollbar; a genuinely short viewport scrolls the same body so every control remains reachable. Tests seed both login providers, assert all sections, auth rows, and footer controls fit, bound section gaps, and scroll to the final Apply control at a tiny viewport. The removed scroll-cue mask and its measurement machinery stay retired. | ZETA-128, ZETA-132 |
| ZETA-139 | Unify content column, shrink current-run dot, uniform font size (arc: GUI polish 3). Henry ran the shipped app and flagged four defects on one screenshot: tool rows spanned nearly full window width while agent prose sat in a narrower centered column, the composer stretched edge-to-edge while transcript rows centered at `TRANSCRIPT_MAX_WIDTH`, the sidebar current-run dot painted as a large filled bullet glued to the first character of the row title, and every text role rendered at a different scaled size. This ticket lands one shared column contract for the whole main view and collapses the size scale to the picked base. Column: every transcript row body (prose, thinking, tool receipts collapsed + expanded, error blocks, turn footer, user turn) and the composer now cap at `theme::prose_body_max_width(base)` inside the shared `TRANSCRIPT_MAX_WIDTH` frame — tool rows shed their pre-fix `wide_body_max_width` cap and join the prose measure; the composer nests inside the same centered flex-col + column + gutter + body wrapper the transcript rows use, so its rail lands under the user-turn rail pixel-for-pixel and its right edge tracks the transcript right edge at both 1100px (column fills viewport) and 1600px (column centers). The pending queued strip also joins the wrapper so a sending message reads at the same shape as a sent one. Dot: `SIDEBAR_CURRENT_DOT_SIZE` shrinks from 9px to 4px so the indicator reads like a mono period, not a bullet; the 4px inset keeps the dot at `left 4px` and the 13px gutter width preserves a 5px gap to the title's first glyph — `current_session_dot_is_subtle_and_leaves_a_gap_before_the_title` pins the size, ratio-to-row-height, and title-gap invariants. Font: `theme::title`, `theme::body`, `theme::label`, `theme::label_small`, `theme::label_micro` all return the base font size at every picker step; hierarchy comes from weight (SEMIBOLD) and color tier (`foreground` / `muted_foreground` / `text_faint`) alone. `settings_scroll_cue_height` re-derives its description-line contribution off the base directly since label_small is no longer smaller. Tests: `role_scale_collapses_onto_one_size_at_every_picker_step` locks the uniform contract at MIN/DEFAULT/MAX; `tool_rows_share_the_prose_body_cap_across_the_picker` iterates collapsed AND expanded receipts at 11/13/18px so both shapes stay inside the prose cap; `composer_left_and_right_edges_match_the_transcript_column` asserts pixel-tolerance alignment across the two viewport widths and the picker's MIN/MAX bases. Existing ZETA-133 shared-edge invariant tests keep passing because the alignment is relative; ZETA-137 marker-column tests keep passing because both the marker and glyph move with the column. Simplicity: no new theme tokens; the type-role helpers stay as named seams but collapse to identity so a future decision to reintroduce a scale lands in one place. Client-only, no protocol / server / RPC changes. | ZETA-133, ZETA-137 |
| ZETA-137 | Transcript inspection — gutter thoughts + clean bash output (arc: GUI polish 2, follow-up to ZETA-133/134/135). Henry reviewed the shipped wiki-look transcript and reported two inspection defects; this ticket lands them on one PR alongside the D3 investigation. D1 (thinking row marker column): pre-fix the whole "+ Thought" string sat as inline body text with `+` at the shared body edge, ~38px right of tool rows' `$` kind glyph — the marker columns were visibly misaligned. `THINKING_HEADER_LABEL` is now split into `THINKING_MARKER = "+"` and `THINKING_HEADER_LABEL = "Thought"`; `ThinkingRowText` carries both fields, and `render_thinking_row` routes the marker into the leading gutter via the SAME gpui-kit Icon-based chevron-slot shape the tool row uses (transparent-color placeholder + label_small semibold `+` at muted-foreground tier), so `+` lands under `$` pixel-for-pixel across the 11px→18px picker range. The header "Thought" starts at the shared body edge alongside `bash` and prose. No other row kind carries a leading marker at the body edge that reads misaligned — error rows keep their danger rail INSIDE the body, turn footer and login rows have no marker. D2 (clean bash receipt): `state::reshape_bash_content` refines its shape — when stdout is the only content and exit is 0, it returns the stdout text ALONE (no `stdout:` header, no `exit: 0` line), matching Henry's "just show the bash output" ask. Labels return when they disambiguate: `stderr:` when stderr is non-empty (and then `stdout:` labels stdout too if both exist), `exit: N` when nonzero. Everything empty still falls through to raw so the receipt is never blank; legacy servers without `structured_content` fall through untouched (the D6 invariant). D3 (thinking body expandable, INVESTIGATED AND DEFERRED — evidence in this row): the provider-stream investigation found that the wire protocol already carries `ContentBlock::Thinking { text }`, but the text is a MIX of raw reasoning and any provider summary: `src/zeta/providers/codex.py` (line 858 raw `reasoning_text.delta` and line 881 `reasoning_summary_text.delta`) both append to `_ItemState.thinking`, which becomes the wire `ThinkingContent.text` (line 1205); `src/zeta/providers/anthropic.py` (line 947-955) forwards Anthropic's raw thinking blocks, which Anthropic's own protocol documents as display-safe but are wire-indistinguishable from Codex's mix. Zeta does not emit a display-safe-ONLY signal on the wire. A minimally-invasive display-safe path requires either (a) splitting `codex.py` to emit ONLY `reasoning_summary_text` into `ThinkingContent` (dropping the raw reasoning from the shared stream), or (b) adding a wire discriminator (e.g. `display_safe: bool` on `ContentBlock::Thinking`). Either path touches provider adapters, the wire type, and the GUI reconcile — beyond a "minimal optional field" — and needs a dedicated ticket. Per the ticket's "if no display-safe channel, do NOT ship raw chain-of-thought" clause, D3 defers to ZETA-137-D3 with the provider-split path as the recommended shape. Tests: `zeta137_thinking_marker_shares_the_tool_kind_glyph_column` (D1) renders a thinking row and a `bash` tool row alone at 1500×1000 across all three picker sizes (11px / 13px / 18px), records `thinking-marker-0`, `thinking-header-0`, `tool-kind-glyph-0`, and `transcript-body` bounds, and asserts: (a) marker.right ≤ body.left (marker in the gutter), (b) marker.left == tool_kind_glyph.left within 1px (columns aligned), (c) thinking body left == tool body left within 2px (shared edge invariant preserved from ZETA-133), (d) header.left matches body.left within 1px tolerance (Thought header sits AT the shared body edge — a `>=` check would let it drift arbitrarily right and still pass); `bash_expanded_shows_output_and_only_labels_when_they_disambiguate` (D2) replaces the pre-fix D6 assertion battery with the new cases: stdout-only + exit 0 → raw stdout text alone (no labels), stdout + stderr → both labeled + no `exit: 0` line, stderr-only + nonzero → labeled stderr + `exit: N`, stdout + nonzero → labeled stdout + `exit: N`, everything empty + exit 0 → fall through to raw, missing `structured_content` → legacy raw untouched. The existing sentinel guard (`thinking_row_paints_a_generic_header_and_never_leaks_private_reasoning`) still holds — the row's `visible_strings()` is now the pair `[THINKING_MARKER, THINKING_HEADER_LABEL]`, both static constants, so no reasoning payload can ever land on any field. The ZETA-133 shared-edge invariant tests still pass because the thinking row's body pair still routes through `transcript_body_pair(...)` with a `LEADING_GUTTER_WIDTH` gutter and `prose_body_max_width` body cap. The ZETA-109 renderer-literal fence still passes — the new visible strings enter through `state.rs` constants and typed `ThinkingRowText` fields; the new `sel::thinking_marker(i)` selector helper joins the existing `sel::*` family. Screenshot: `docs/screenshots/ZETA-137-before.png` shows a general pre-fix transcript state (markdown, tool table, code block, collapsed tool-call and bash receipts) — no thinking row is present and the bash receipt is collapsed, so the shot does not directly evidence D1's marker column drift or D2's expanded receipt reshape; the test assertions above are the load-bearing evidence, and the after / thought-expanded shots did not ship on this PR. Simplicity pass: `reshape_bash_content` now has one early-return for the clean case + one early-return for the empty case + one labeled path, folding the two pre-fix trailing-newline strip loops into one path; `render_thinking_row` gains ~10 lines for the gutter shape and reuses the tool row's chevron-slot pattern verbatim, so the alignment invariant lives in ONE gpui-kit Icon call rather than a hand-rolled spacer with drifting padding math. Client-only: no protocol or server changes. | ZETA-133, ZETA-134, ZETA-135 |

## Deferred / open followups (not yet ticketed)

- **ZETA-137-D3 (thinking traces expandable, PROVIDER SPLIT REQUIRED)**: `ContentBlock::Thinking { text }` currently mixes raw reasoning and provider summary; retaining a display-safe body requires splitting `codex.py` so only `reasoning_summary_text` events emit `ThinkingContent` (raw reasoning stays server-side), OR a wire discriminator on `ContentBlock::Thinking`. The provider-split path is the recommended shape — smaller wire surface, cleaner privacy contract. When picked up: (a) codex.py `_ItemState.thinking` splits into `summary_text` (safe) and `raw_text` (private), (b) `_complete_item` emits `ThinkingContent(item.summary_text, item.encrypted_content)` from summary only, (c) `TranscriptEntry::Thinking` grows an optional `body: Option<String>`, (d) `render_thinking_row` renders expandable when `body.is_some()` using the ZETA-135 inset panel shape (the D1 gutter marker already doubles as the expand affordance), (e) test with a real streamed summary payload plus the ZETA-107 sentinel guard for the raw channel. Investigation and evidence: see the ZETA-137 row above.
- **Unfork `gpui-base` vendor copy (ZETA-129)**: `gui/vendor/gpui-base/` holds a two-edit fork of `gpui-base` 0.6.1: (1) the functional `InlineFlow::prepaint` swap that passes `AvailableSpace::MaxContent` on the width axis so the inner `StyledText` cannot re-wrap a chip whose shape already fits (env-toggled by `ZETA_GUI_INLINE_FLOW_DEFINITE` for the CI mutation arm), and (2) a `#[cfg]`-gated recorder + probe in `InlineFlow::prepaint` that pushes each inner-text fragment's wrap-boundary count into `zeta129_wrap_recorder` so the paired mutation test can assert "an inline text fragment NEVER wraps inside its own fragment"; the recorder compiles out of release builds. Delete the directory and the `[patch.crates-io.gpui-base]` entry in `gui/Cargo.toml` once a released `gpui-kit` version depends on a `gpui-base` release carrying the equivalent fix (in whichever shape upstream ships — `MaxContent`, a wrap tolerance in `compute_wrap_boundaries`, or a different call site). Full rationale + both diffs in `gui/vendor/README.md`.
- **Unfork `gpui-component` vendor copy (ZETA-133-D3)**: `gui/vendor/gpui-component/` holds a one-edit fork of `gpui-component` 0.6.1 — a `MessageScrollerState::new_with_alignment(item_count, alignment, cx)` seam so the transcript can request `ListAlignment::Bottom` for short-transcript bottom anchoring. `new` delegates to `new_with_alignment(_, ListAlignment::Top, _)`, so every existing caller keeps shipped behaviour byte-for-byte. Delete the directory and the `[patch.crates-io.gpui-component]` entry in `gui/Cargo.toml` once a released `gpui-kit` depends on a `gpui-component` release that exposes alignment on `MessageScrollerState` (a constructor argument, an `on_alignment` builder, or a runtime setter). Full rationale + diff in `gui/vendor/README.md`.
- **Residual login-test flake**: `tests/test_login.py::test_login_sigint_closes_callback_server` failed once under full-suite load with concurrent filesystem writes (2026-09-08, post-ZETA-91, post-ZETA-88-round-2; 5/5 green isolated). One occurrence; fold into the next deflake round if it recurs.
- **zeta serve session hardening**: a killed serve can leave a half-written session dir (`conversation.jsonl` without `meta.json`) that `status` still advertises as current, wedging clients on resume (hit live 2026-09-08; client-side recovery shipped in #131). Server side: create session dirs atomically (write meta.json via tmp+rename before advertising) and exclude unopenable sessions from `status`/`list_sessions`.
- **Prompt-cache management**: ZETA-23 landed usage observability, cache-aware accounting, and /status hit rate. Explicit breakpoint tuning and richer observability UI remain deferred.
- **Sub-agents, slash-command DSL**: sub-agents landed via ZETA-37..49; the slash-command DSL is now ticketed as ZETA-53..55 (macro DSL arc).
- **MCP**: OAuth / token-refresh auth flows and resources-as-attachments deferred; QoL arc (observability, config ergonomics, resilience, prompts) ticketed as ZETA-56..59.
- **Rejected (not planned)**: third providers beyond Anthropic + Codex; thinking-mode / reasoning-effort controls in the assistant; sandbox for `exec`/`bash` (LLM is trusted; user retains approval policy for consequential ops).
- **Display-safe thinking summaries**: zeta's provider protocol has no display-safe reasoning channel — `ContentBlock::Thinking` currently mixes raw reasoning with any model-emitted summary (see the Thinking assembly in `src/zeta/providers/codex.py`; Anthropic raw thinking arrives the same way). Until the server exposes a separate summary channel distinct from raw reasoning, the GUI must render only a header-only "+ Thought" marker; expanded thinking bodies, per-turn duration, and titled headers stay blocked on that server-side work.
- **Exhaustive typed row-text model + renderer-literal fence for the GUI transcript (ZETA-107 follow-up)**: DONE — shipped as ZETA-109 (round 2 raised the guard to an AST-based fence in a single-module render surface after r1 flagged content-heuristic bypasses). The positional `visible_text` seam is replaced by the `row_text::RowText` / `LoginRowText` struct-per-variant model. All six transcript renderers plus the login row now live in one `transcript_render` module scanned by a `syn`-based fence that rejects any string / byte-string literal outside `.debug_selector(...)` / `.id(...)` / `.aria_label(...)` / `.role(...)` args and diagnostic-macro payloads. Field-drop caught by `-D warnings`; stray literals caught by AST context (not string shape) so `.child("done")`, `format!("hello {n}")`, byte strings, moving text into a fresh helper fn, and renaming a renderer all remain covered.

Gate: `uv run pytest -q`. Review flow: same luna implementer -> sol reviewer
loop as the wiki repo; merges by the orchestrator after a clean pass.


### GUI model entitlement recovery (ZETA-100)

The Claude and Codex adapters have a static model catalog, not an account-specific
entitlement API. A catalog entry does not prove that the stored credentials can
use the model. Apply therefore does not issue a completion, including a token-limited
probe. It saves the previous provider, model, and context budget in session metadata.
Repeated changes preserve that original fallback until a provider response completes
or the user returns to that same provider/model, which clears it.
The fallback survives reconnects, session changes, and server restarts.

On a foreground authentication or model-access error before that first response,
the server restores the fallback backend, model, context budget, and persisted
session settings. The error block says which model was restored and retains the
provider error. Approval mode remains the user's selected value. Transport,
rate-limit, MCP setup, and background-agent errors do not revert the model.
Classification uses the completion error's code and HTTP status, never message text.
HTTP 400/401/403/404 and structured authentication/access codes qualify. A 400
from a newly selected model can also mean invalid input; recovery conservatively
restores the previous model without trying to interpret provider prose.
A successful response clears the fallback. This does not retry or resend a message.
If restoring the fallback fails (for example, its credentials were removed), the
error explains the failure and directs the user to Settings.

No new RPC or protocol version is required: `set_settings` remains gated on the
existing 1.1 session extension. A 1.0 server gets no settings request; its error
blocks omit the unavailable Settings action. Fake servers keep their sealed
catalog and do not create entitlement fallbacks. Fallback metadata stays internal
and does not appear in session responses at either protocol version. Only 1.1
clients receive `model_access_error` or `model_reverted` recovery codes; 1.0
clients retain the original error code and message.


### GUI browser login (ZETA-101)

Protocol 1.1 servers advertise `login_providers`, `login_start`, `login_status`,
and `login_cancel` only for real providers. Clients require all four capabilities;
older 1.1 servers also remain compatible. Login requests do not require an active
session. Providers use the session names `claude` and `codex` on the wire.

`login_providers` returns each provider's `credentials_present` flag. This checks
local OAuth stores and the existing bootstrap sources without token refresh or
an account probe. The GUI says "Credentials available" because presence does not
prove that credentials are current or that an account can use a given model.
Unreadable credentials show the login action as available.

`login_start` returns `state: pending` and `authorization_url` after the existing
PKCE redirect listener binds. The GUI opens that URL in the default browser and
polls `login_status` every 500 ms while pending. There is one task per provider,
owned by the connection. A second start returns RPC error `-32005` with
`data.code: login_in_progress`. Providers can log in independently. Results remain
available until the next start; disconnect cancels all pending logins.

The five-minute deadline covers the callback and token exchange. Terminal states
are `succeeded`, `cancelled`, and `failed`; an untouched provider reports `idle`.
Failures include a typed error (`login_timeout`, `login_callback_error`, or
`login_failed`) and safe user text. Cancel waits for listener cleanup before
returning. No provider exception details, tokens, or callback codes reach the GUI.

Foreground provider authentication errors carry `data.login_provider` on the
1.1 wire. The server captures the failed provider before existing model recovery
runs. MCP, transport, and background-agent errors do not offer provider login.
Success permits a manual retry; it never resends a message automatically.

### Session management (ZETA-103)

Protocol 1.1 advertises `rename_session` and `delete_session`. Both accept
`session_id` (an exact ID or unique prefix); rename also accepts a string `name`.
Whitespace clears the stored name. Names follow the existing 60-cell limit.
Listings expose `name` only to 1.1 clients and keep `first_message_preview`
independent of the display name. The GUI requires both advertised requests.

Rename remains available while a turn streams. Deletion requires an idle
server. Deleting the active session returns RPC
`-32005` with `data.code = "active_session"`; select another session first.
Deletion does not read metadata or conversation data, so corrupt directories
remain removable by ID. It refuses root/session/lock symlinks, respects the
session lock, and uses fd-based recursive deletion without following nested
links. `zeta session rename ID NAME` also accepts an empty name to clear it.
